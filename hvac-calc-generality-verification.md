# 計算エンジン 汎用性検証・仕様確認レポート

作成日: 2026-09-02
方針: コードは変更していません。「入力条件が変わっても正しく計算が変化する汎用的なエンジンになっているか」の検証が今回の主眼です。

---

## ① 244W/㎡の正体(再確認)

**計算結果ではなく、開発初期に外部根拠なしで直接コードに書き込んだ固定リテラルです。** `BUILDING_TYPES`配列の`restaurant`エントリの`coolingWm2: 244`という記述そのものが数値の出処であり、他の式から導出された値ではありません。

## ② 24.4kWが生成される全計算過程

```
入力: floorAreaTotal=100, buildingTypeId="restaurant", regionId="kanto", coolingSetTemp=26
  ↓
buildingType.coolingWm2 = 244      (BUILDING_TYPESからの参照。固定値)
region.coolingFactor = 1.0          (REGIONSからの参照。固定値)
tempFactorCooling = 1 + (26-26)×0.03 = 1.0   (設定温度が基準値26℃と同じため1.0)
  ↓
roughLoadCoolingKW = 100 × 244 × 1.0 × 1.0 ÷ 1000 = 24.4kW
```

**一段階の掛け算のみ。中間の負荷積み上げは存在しません。**

## ③ 現在実装されている全計算式

`hvac-calc-engine.js`のSHARED-LOGICブロック(jsxと一字一句同一、diff検証済み)より:

| 段階 | 計算式 |
|---|---|
| 概算負荷(冷房/暖房) | `area × Wm2係数 × 地域係数 × 温度補正係数 / 1000` |
| 温度補正係数 | `1 ± (設定温度差) × 0.03` |
| 人体負荷(参考値) | `occupants × 60(顕熱)/50(潜熱) / 1000` |
| 設計用必要負荷 | `概算負荷 [+人体負荷(オプトイン時のみ)] × (1+計画余裕%/100)` |
| 換気量 | `occupants × 用途別原単位` |
| 必要容量 | `max(冷房設計用必要負荷, 暖房設計用必要負荷)` |
| 機種台数 | `ceil(必要容量 ÷ 機種容量)` |
| 余裕率 | `(設置合計容量-必要容量)/必要容量×100` |

**入力として受け付けているのは`buildingTypeId`・`regionId`・`floorAreaTotal`・`floors`(計算に不使用)・`occupants`・`coolingSetTemp`・`heatingSetTemp`・`marginPct`・`includeOccupantLoad`の9項目のみです。窓面積・方位・照明・機器発熱・湿度・天井高を受け取る引数自体が関数シグネチャに存在しません。**

## ④⑤ 各係数・条件の根拠、正式資料・出典・該当箇所

| 係数/条件 | 根拠 | 出典 | 該当箇所 |
|---|---|---|---|
| 面積×W/㎡という計算方法 | 業界の実在する簡易選定方法と構造一致 | ダイキン工業公式サイト「容量(馬力)から選ぶ」 | https://www.ac.daikin.co.jp/shopoffice/info/pw(2026-09-02確認) |
| 244W/㎡(飲食店) | ダイキン公式レンジ190-370W/㎡内(結果的な整合、当初この値を参照して設定したものではない) | 同上 | 同上「飲食店」セクション |
| 172W/㎡(オフィス) | ダイキン公式レンジ105-230W/㎡の中央値に近い | 同上 | 同上「一般事務所」セクション |
| 209W/㎡(店舗物販) | ダイキン公式レンジ160-180W/㎡を**超過**(不整合) | 同上 | 同上「一般商店」セクション |
| 病院・ホテル・学校・工場・集合住宅の原単位(5用途) | **未確認** | ― | ― |
| 熱負荷の対象項目(構造体・ガラス・日射等) | 定義のみ確認、数値なし | 国交省「建築設備設計基準」令和6年版第4編第1章第2節 | mlit.go.jp/gobuild/content/001390961.pdf |
| すきま風算定式 | 式は明記されているが未実装 | 同上 | 同上 |
| 機器定格試験条件 | 一致(能力補正自体は未実装) | JRAIA公式サイト | 前回セッション確認 |
| 温度設定補正(±3%/℃) | **根拠なし** | ― | ― |
| 窓・方位・照明・機器発熱の扱い | **入力項目自体が存在しないため、根拠の有無を論じる以前の状態** | ― | ― |

---

## ⑥ 建物用途・階数・面積・窓・方位・人数・内部発熱が変わった場合の挙動検証(今回の核心)

実際にコードを実行して検証しました。

| 検証内容 | 結果 |
|---|---|
| 用途を変える(病院→飲食店、同じ100㎡) | 病院14.0kW→飲食店24.4kW。**適切に変化します**(用途別原単位が切り替わるため) |
| 階数を変える(3階→11階、同じ用途・面積) | 14.0kW→14.0kW。**変化しません**(階数は意図的に計算対象外という仕様のため。前回セッションで確定済みの仕様どおり) |
| 面積を変える | 単調増加を確認済み(前回の自動テストで検証済み) |
| 人数を変える | 換気量には反映される。設計用必要負荷には**既定では反映されない**(includeOccupantLoad=falseのため) |
| 窓面積・方位・照明・機器発熱を計算に渡す | **無視されます。これらを受け取る仕組み自体が実装されていません。** 存在しないパラメータを追加で渡しても計算結果は一切変化しないことを実際に確認しました |

**結論**:「3階の病院」「11階の飲食店」のように**用途・面積が変われば結果は正しく変わります。** しかし、**窓の大小、方位、照明・機器発熱の違いは、そもそも入力する手段がないため、どんな値を想定しても計算結果に反映されません。** これは「汎用的な計算エンジン」としては明確な欠落です。プロが実務で使った場合、「南向き全面ガラスの店舗」と「北向き小窓の店舗」が同じ面積・用途であれば**同じ数値が出てしまいます。** これは个別建物条件を反映すべき場面で反映できていないという、最も重要な指摘です。

---

## ⑦ プロ向け既存ツールと比較した不足要素

日本の専業技術者が実務で使う代表的な熱負荷計算プログラム「HASP/HASPEE」(空気調和・衛生工学会公式)を確認しました。同ツールは:
- 方位別(東西南北)に異なる負荷を持つ室を区別して計算(例:標準モデルの「西側事務室」「東側事務室」)
- 気象データに基づく時刻別(非定常)動的計算
- 人体発熱の顕熱:潜熱比を設計室温基準で設定可能
- 空調運転スケジュール(<OPCO>)による間欠空調時の蓄熱負荷計算
- 気象条件表・熱貫流率表・各室熱負荷計算書・空気線図・熱源集計表という一連の計算書一式を出力

**本エンジンとの比較**:

| 項目 | HASP/HASPEE(プロ向け) | 本エンジン |
|---|---|---|
| 方位別負荷 | 対応(東西南北で個別計算) | **非対応(方位という概念自体がない)** |
| 時刻別動的計算 | 対応 | **非対応(定常・単発計算のみ)** |
| 窓面積・日射 | 対応 | **非対応** |
| 人体発熱の顕熱潜熱比 | 設計者が設定可能 | 固定値(60W/50W、出典は非公的) |
| 蓄熱・間欠運転 | 対応 | **非対応** |
| 計算書の出力(各室別・熱源集計) | 対応 | 非対応(単一の結果表示のみ) |
| 用途別の簡易目安(W/㎡) | 提供せず(積み上げ計算が前提) | 提供(ダイキン公式と概ね整合) |

**結論**:HASPと比較すると、本エンジンは「プロ向けの精密な負荷計算ツール」ではなく「初期概算専用のツール」の位置づけです。これは前回までのレビューと同じ結論ですが、今回、方位・窓・蓄熱という具体的な欠落項目を、実在するプロ向けツールとの比較で改めて特定できました。

---

## ⑧ 「確認済み」「標準値」「推定値」「未入力」の区別ができているか

**部分的にできています。** `DataTag`コンポーネント(`real`/`sample`/`estimate`/`ref`/`check`)により、機器データについては実データとサンプルデータを明確に区別する仕組みが既に存在します。しかし、**負荷計算の入力値そのもの(温度・人数等)については、「これはユーザーが確定的に入力した値か、アプリの標準値をそのまま使っているか」を区別する仕組みがありません。** 例えば、ユーザーが冷房設定温度を一切変更しなくても、入力欄には既定値26℃がそのまま入っており、「26℃はユーザーの確定入力」なのか「未入力なのでアプリの標準値26℃を使っている」のかを、現在のデータ構造は区別できません。

---

## ⑨ 修正が必要な箇所

1. **窓面積・方位・照明・機器発熱を計算に反映する仕組みの新設**(現状は入力手段自体が存在しない、最も重要な欠落)
2. **店舗(物販)209W/㎡ → ダイキン公式レンジ160-180W/㎡内への修正**
3. 病院・ホテル・学校・工場・集合住宅の原単位(5用途)の出典調査
4. 「確定入力/標準値/推定値」を区別するデータ構造の導入(現在は入力値と既定値が区別なく同列に扱われている)
5. 温度設定補正(±3%/℃)の根拠調査、または廃止

## ⑩ 現状のままでよい箇所

1. 面積×用途別原単位という計算方法自体(ダイキン公式と構造一致)
2. 244W/㎡(飲食店)・172W/㎡(オフィス)の数値自体(実在レンジと整合)
3. 用途・面積が変われば結果が正しく変化する点(動作を確認済み)
4. 階数を計算対象外とする仕様(前回確定済み、条文上の矛盾なし)
5. 実在機器と容量クラス仮選定の区別構造(selectionType)
6. 安全率を機械的に掛けない設計構造

---

## ⑨' 初回ローンチ版で確定すべき計算仕様

「プロが実務で使っても『この条件が計算に入っていない』とならない」という基準に照らすと、**最低ラインとして次の2つが必須**と判断します。

1. **窓面積・方位を入力できる構造にし、日射負荷を(概算でよいので)計算に反映すること。** これがないと、外皮条件が全く異なる建物が同じ数値を出してしまい、プロの目にはすぐに見抜かれます。
2. **照明・機器発熱を入力できる構造にすること。** 特に厨房を持つ飲食店では、これが欠けていると現場感覚と大きく乖離します。

逆に、時刻別動的計算・蓄熱計算(HASPが行っているレベル)は、**初回ローンチ版の最低ラインとしては必須ではない**と判断します(概算ツールとしての位置づけを明示すれば許容範囲)。

## ⑩' コード変更の必要性

**あります。** ただし今回は仕様確定のみで、コードは変更していません。次工程で以下が必要です。
- `Window`・`Envelope`(方位別窓面積・簡易U値または遮蔽係数)の入力構造追加
- `Lighting`・`Equipment`(照明・機器発熱)の入力構造追加
- これらを反映する日射負荷・照明負荷・機器発熱負荷の計算式追加(前回の詳細モード仕様書v1.0で設計済みの内容の実装着手)
- `BUILDING_TYPES`の店舗(物販)係数修正

## ⑪ テスト結果

今回、以下を実際にコード実行して検証しました(結果はすべて上記の通り)。
- 用途変更(病院⇔飲食店)で結果が変化することを確認 ✅
- 階数変更(3階⇔11階)で結果が変化しないことを確認(仕様どおり)✅
- 窓面積等の未実装パラメータを渡しても結果が一切変化しないことを確認 ✅(=欠落の証明)

既存の31件の自動テストは変更・削除していません。今回は新規の探索的検証(3件)を追加実行しましたが、正式なテストスイートへの追加は行っていません(コード変更を伴わないため)。

## ⑫ 現在の開発工程

引き続き詳細設計〜実装〜単体テストの間にあります。今回、「詳細モード(窓・方位・照明・機器発熱の個別計算)が実装されていない」ことが、初回ローンチの最低ラインに対する**具体的な未達項目**として特定されました。

## ⑬ 次に実施すべき作業

新機能(UI・図面認識・音声・価格比較)には着手せず、**⑩'のコード変更(窓・方位・照明・機器発熱の入力構造と計算式の追加)を計算エンジンの拡張として実施することを推奨します。** これは前回までの「詳細モード仕様書v1.0」で設計済みの内容の一部であり、新規設計ではなく、既存設計の実装着手という位置づけになります。
