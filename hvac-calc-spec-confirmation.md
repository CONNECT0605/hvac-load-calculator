# 計算仕様確定レポート

作成日: 2026-09-02
方針: コードは変更しません。「何を正しい計算として実装するか」の仕様確定のみ行います。既存31件のテストは維持します。

---

## ① 244W/㎡の正体

**計算結果ではありません。開発初期にコード内へ直接書き込まれた固定リテラル(仮定値)です。** `BUILDING_TYPES`配列内の`{ id: "restaurant", coolingWm2: 244, ... }`という定義そのものが数値の出処であり、他の計算式から導出されたものではありません。

---

## ② 24.4kWが生成される元の計算式

```
roughLoadCoolingKW = floorAreaTotal × coolingWm2 × region.coolingFactor × tempFactorCooling / 1000
                    = 100 × 244 × 1.0(関東) × 1.0(冷房26℃=基準温度) / 1000
                    = 24.4 kW
```

中間計算・要素積み上げは一切なく、**単一の掛け算の結果**です。244・地域係数・温度補正係数のいずれも、他の下位計算から求められたものではなく、すべて直接の入力係数です。

---

## ③ 現在の全計算式(engine.js SHARED-LOGICブロック)

| 計算 | 式 |
|---|---|
| 概算負荷(冷房) | `roughLoadCoolingKW = area × coolingWm2 × coolingFactor × tempFactorCooling / 1000` |
| 概算負荷(暖房) | `roughLoadHeatingKW = area × heatingWm2 × heatingFactor × tempFactorHeating / 1000` |
| 温度設定補正(冷房) | `tempFactorCooling = 1 + (26 − coolingSetTemp) × 0.03` |
| 温度設定補正(暖房) | `tempFactorHeating = 1 + (heatingSetTemp − 22) × 0.03` |
| 人体顕熱/潜熱(参考値) | `occupants × 60/1000` kW、`occupants × 50/1000` kW |
| 設計用必要負荷 | `roughLoad [+ 人体負荷(オプトイン時のみ)] × (1 + marginPct/100)` |
| 換気量 | `occupants × ventPerPerson` |
| 必要容量 | `max(設計用必要負荷 冷房, 暖房)` |
| 機種台数 | `ceil(必要容量 ÷ 機種容量)` |
| 余裕率 | `(設置合計容量 − 必要容量) / 必要容量 × 100` |

---

## ④⑤ 各計算式の根拠・正式資料・章節・ページ

**追加調査により、公的資料だけでなく実際の空調業界の簡易選定慣行を確認しました。** これは「既存の業務用空調計算・積算ツールと比較して劣らない」という目標に直接関わる、重要な追加情報です。

| 計算式/係数 | 根拠資料 | 該当箇所 | 位置づけ |
|---|---|---|---|
| 面積×W/㎡という計算方法そのもの | **ダイキン工業公式サイト「容量(馬力)から選ぶ」ページ**(2026-09-02確認、https://www.ac.daikin.co.jp/shopoffice/info/pw)。同ページは「算出基準負荷(W/㎡) × 面積 = 必要能力」という、**本エンジンと全く同一の計算方法**を採用している | ダイキン工業公式サイト | **これは実在する主要メーカー自身が採用する簡易選定方法であり、「本アプリの方法が競合ツールに劣る簡略化」ではなく、「競合・メーカー自身が使う標準的な簡易選定方法と同一構造」であることが確認できました** |
| 244W/㎡(飲食店・冷房) | ダイキン公式サイト:飲食店の算出基準負荷は**190〜370W/㎡** | 同上「飲食店」セクション | **244はこの実在レンジ内に収まっています。** ただし当初この値がこのレンジを参照して設定されたわけではなく、結果的に整合していたに過ぎません(偶然の一致) |
| 172W/㎡(オフィス・冷房) | ダイキン公式サイト:一般事務所は**105〜230W/㎡**(中央値約167.5) | 同上「一般事務所」セクション | **172は中央値に極めて近く、良好な整合** |
| 209W/㎡(店舗・物販・冷房) | ダイキン公式サイト:一般商店は**160〜180W/㎡** | 同上「一般商店」セクション | **209はこのレンジを超過しており、不整合。** 過大評価の可能性 |
| 温度設定補正(±3%/℃) | 確認できる資料なし | ― | **根拠なし。本アプリ独自の仮定のまま** |
| 機器定格試験条件(能力補正の基準点) | 日本冷凍空調工業会(JRAIA)公式サイト(前回セッション確認済み):冷房室内27℃DB/室外35℃、暖房標準室内20℃/室外7℃ | JRAIA公式サイト | 一致(ただし能力補正曲線自体は未実装) |
| 熱負荷計算の対象項目(構造体・ガラス・日射等) | 国土交通省「建築設備設計基準 令和6年版」第4編第1章第2節 | mlit.go.jp/gobuild/content/001390961.pdf | 前回確認済み。個別数値ではなく計算対象項目の定義のみ |
| すきま風負荷の算定式 | 同上 | 同上 | 前回確認済み、算定式明記あり(未実装) |

---

## ⑥ 競合レベルの業務計算として不足している項目

ダイキン公式の簡易診断ツールと比較すると、**計算方法の骨格は同等ですが、以下が不足しています。**

| 項目 | ダイキン公式の対応 | 本アプリの現状 |
|---|---|---|
| 用途カテゴリの網羅性 | 4カテゴリ(一般商店/理美容院/飲食店/一般事務所)を公式レンジで提供 | 8カテゴリだが、ダイキンが提供する4カテゴリ以外(病院・ホテル・学校・工場・集合住宅)は根拠未確認のまま |
| 数値のレンジ表示 | 「190〜370W/㎡」とレンジで提示し、幅があることを利用者に伝えている | 単一の固定値(244)のみで、変動幅を提示していない |
| 実在機種への接続 | 診断結果から直接、実在の型式・容量ラインアップに接続 | 5.6kW・8.0kWクラスのみ実在接続、他は未接続 |
| 図面・写真からの情報取得 | なし(手入力のみ、本アプリと同水準) | なし(今回スコープ外) |
| 不足情報の確認フロー | なし(本アプリと同水準) | なし(今回スコープ外) |
| 確定値/標準値/推定値の区別 | 明確な区別なし(本アプリの「要確認」タグの方が優れている点) | 一部実装済み(DataTag機構) |
| 価格・仕入先・納期比較 | 別ページで「価格相場の目安」を提示するのみ、比較機能はなし | サンプルデータで比較UIは実装済み(実データは未接続) |
| 電気代・ランニングコスト比較 | なし | 未実装(今回スコープ外) |

**結論**:計算方法の骨格(面積×原単位)自体は競合・メーカー公式ツールと同水準です。**劣っているのは、原単位の実在レンジ網羅性(8用途中4用途のみ実在根拠あり)と、実在機器データの容量クラス網羅性(7容量クラス中2クラスのみ)です。**

---

## ⑦ 現在の計算で修正が必要な項目

1. **店舗(物販)の209W/㎡**:ダイキン公式レンジ(160〜180W/㎡)を超過。過大評価であり、レンジ内への修正が必要
2. **病院・ホテル客室・学校・工場・集合住宅の原単位(5用途)**:根拠となる実在データを未確認のまま。ダイキンの公式ページには該当カテゴリがなく、別途調査が必要
3. **単一固定値ではなくレンジでの提示**:ダイキン方式にならい、「190〜370W/㎡」のような幅を持たせ、利用者が保守的/標準的のどちらを取るか選べる構造が望ましい
4. **温度設定補正(±3%/℃)**:根拠不明のまま。実務上の妥当性を別途検証するか、廃止を検討

## ⑧ 現在の計算で維持すべき項目

1. **面積×原単位という計算方法そのもの**:ダイキン公式が採用する方法と構造的に同一であり、変更不要
2. **244W/㎡(飲食店)・172W/㎡(オフィス)**:実在レンジと整合しており、当面維持可能(出典を追記の上で)
3. **人体負荷を暖房に加算しない設計**:建築設備設計基準の考え方と整合
4. **安全率を機械的に掛けない設計(計画余裕を任意入力とする構造)**:国交省基準が特定係数を強制していないことと整合
5. **実在機器と容量クラス仮選定を明確に区別する構造(selectionType)**:競合の多くが「馬力の目安」止まりで実在型式まで接続していない中、本アプリの強みになり得る

---

## ⑨ 初回ローンチ版として必要な計算仕様(確定)

1. 用途別原単位は、**根拠を確認できたもの(飲食店・オフィス・店舗)から順にダイキン公式レンジへ更新し、出典を明記する**
2. 根拠未確認の5用途(病院・ホテル・学校・工場・集合住宅)は、**「要確認」ステータスを維持したまま、個別に業界資料を調査する**
3. 温度設定補正・地域係数は、**「本アプリの概算仮定」である旨を画面表示で維持**(詳細モード実装まで)
4. 実在機器データは、**残り5容量クラス(7.1/11.2/14.0/16.0/22.4kW)の調査を優先**
5. 概算負荷を**単一値ではなくレンジ(下限〜上限)で提示する構造への変更を検討**(ダイキン方式との整合性向上)

## ⑩ 必要なコード修正(次工程で実施。今回は未実施)

1. `BUILDING_TYPES`の`coolingWm2`/`heatingWm2`に、出典・確認日を保持するメタデータフィールドを追加
2. `retail`(店舗物販)の`coolingWm2`を209→160-180のレンジ内(例:170)に修正
3. 可能であれば`coolingWm2`を単一値から`{min, max}`のレンジ構造に変更し、UIでレンジ表示できるようにする
4. 5用途分の原単位について、追加調査後に数値を更新

## ⑪ 修正後に必要なテスト

1. 既存31件のテスト:`retail`の係数変更に伴い、店舗関連の期待値がある場合は更新が必要(現状、既存テストは`restaurant`ベースが中心のため直接の影響は限定的と見込まれるが、要確認)
2. レンジ構造への変更を行う場合、`roughLoadCoolingKW`が単一値かレンジかで戻り値の型が変わるため、下流(機器選定・UI)への影響を確認するテストを追加
3. 出典メタデータフィールド追加後、全`BUILDING_TYPES`エントリにメタデータが揃っていることを検証するテスト(前回のEQUIPMENT_DBと同様の網羅性テスト)

---

## ⑫ 現在の開発工程

前回の開発工程監査(11段階評価)から更新はありません。**詳細設計〜実装〜単体テストの間**にあります。今回のレビューにより、計算仕様確定の材料(ダイキン公式データという実在根拠)が新たに得られましたが、**コードへの反映はまだ行っていません。**

## ⑬ 次工程

新機能追加ではなく、⑩のコード修正(用途別原単位の実在根拠への置き換え)を優先することを推奨します。特に店舗(物販)の過大評価(209→160-180W/㎡)は、他の値より修正の必要性が明確です。
