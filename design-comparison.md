# デザインA/B/C 比較と最終決定

対象: 空調負荷計算Webアプリ(10ステップ入力 → 計算 → 帳票/CSV)
比較方法: 実ブラウザ(Chromium)で同一案件を操作し、認知負荷に関わる指標を実測。
計算ロジック・入力項目・データモデルは3案で完全に共通(差分はレイアウトのみ)。

- 実装: `design-variants.jsx`
- 切替: `?design=A` / `?design=B` / `?design=C`(未指定は最終採用デザイン)
- 回帰検証: `e2e/design-matrix.spec.mjs`(3案すべてで機能等価性を検証)

## 1. 3案の構造

| | A: 一望型(旧来) | B: 段階誘導型 | C: 集中型 |
| --- | --- | --- | --- |
| PC構成 | 3カラム(ナビ260 / 入力 / 状況300) | 2カラム(入力 / 状況300) | 1カラム(入力) + 下部固定バー |
| ステップ導線 | 左に10項目を常時縦並び | 3段(つくる/あわせる/だす) + 10項目の横並び | 下部バーの「全ステップ」 |
| 状況表示 | 右に常時(進捗・不足・計算値・天気・適用範囲) | 右に常時(不足件数・主要3値) + 追従 | 下部バーに不足件数と計算値を要約 |
| 現在地 | 左ナビのハイライト | 段タブ + 番号ストリップ | 下部バーに「n/10 名称」 |

## 2. 実測値

同一案件(3階・3室・各150m²・事務室)を操作して計測。

| 指標 | A | B | C |
| --- | --- | --- | --- |
| 初期画面の縦長さ PC (px) | 1233 | **1090** | **1077** |
| 初期画面の縦長さ タブレット (px) | 2183 | 1434 | **1077** |
| 初期画面の縦長さ スマホ (px) | 2237 | 1520 | **1092** |
| 現在ステップが初期表示で見える | ○ | ○ | ○ |
| 不足件数が初期表示で見える | ○ | ○ | ○ |
| 横スクロール(PC/タブレット/スマホ) | 0px | 0px | 0px |
| 10工程への直接移動 | 1クリック | 1クリック | 2クリック(バー展開) |
| 計算実行への最短到達 | 9クリック | 9クリック | 9クリック |

### 読み取れること

- **A は情報量は最大だが縦に長い。** タブレットで2183px(画面2.1枚分)、スマホで2237px。
  左ナビが全展開されるため、入力にたどり着く前にスクロールが必要になる。
  一度に全部見える「一望性」は長所だが、常用時の移動コストが高い。
- **B は縦長さを約34%(タブレット)削減。** 段タブで「いま何をすべきか」が常に見え、
  現在の段の残り件数も同時に見える。視線は入力→段タブ→右状況の3点で完結する。
- **C は最も短いが、計算値と不足件数が下部バーの要約のみ。** 個別の指摘内容(どの室の
  何が未入力か)を見るには別途どこかへ移動する必要があり、修正時の手戻りが増える。
  また10工程への直接移動にバー展開の1クリックが余分にかかる。

## 3. 評価(プロユーザーが最短時間で正確に入力・確認・修正できるか)

| 観点 | A | B | C |
| --- | --- | --- | --- |
| 情報認知(全体像の把握) | ◎ | ○ | △ |
| 入力速度(移動コスト) | △ | ◎ | ○ |
| 視線誘導(次に見る場所) | △(3カラムで分散) | ◎(列が1本) | ○ |
| エラー防止(不足の見え方) | ◎(指摘を全件列挙) | ◎(件数+上位4件) | △(件数のみ) |
| 修正のしやすさ | ◎ | ◎ | △ |
| プロ用途適性 | ○ | ◎ | ○ |
| PC適性 | ○(縦長) | ◎ | ○ |
| スマホ適性 | △(縦長2237px) | ◎(1520px) | ◎(1092px) |
| 既存機能の維持 | ◎ | ◎ | ◎ |

## 4. 最終決定

**Design B(段階誘導型)を採用する。**

理由:

1. **認知負荷**: 10項目をいきなり並べるのではなく「つくる→あわせる→だす」の3段に
   まとめることで、初見でも現在地と次にやることが1箇所で分かる。一方で10項目への
   直接移動も残しているため、慣れた担当者の操作速度を落とさない。
2. **視線誘導**: 入力が1列にまとまり、次に読む場所(段タブ→入力→状況)が一意になる。
   A の3カラムは視線が分散し、C は状況が下部に退いて参照のたびに目線移動が大きい。
3. **エラー防止**: 不足件数を常時表示し、上位の指摘内容まで見える。C は件数のみで
   内容確認に移動が必要なため、修正漏れのリスクが残る。A と同等の情報量を保っている。
4. **スマホ**: 縦長さを A 比で約32%削減しつつ、情報の欠落がない(不足・計算値・天気・
   適用範囲をすべて維持)。320px でも横スクロール0px。
5. **既存機能の維持**: 入力項目・計算・帳票・CSV・天気・保存は3案で完全に同一。
   切り替えによる機能差はないことを E2E で機械的に検証している。

B の唯一の弱点(長い入力画面で状況が上に流れる)は、PC で右カラムを追従(sticky)させる
ことで補った。狭幅では追従を解除し、入力エリアを狭めない。

## 5. 採用しなかった案の扱い

A と C は削除せず、`?design=A|C` で参照できる状態で残す。理由:

- A は従来のレイアウトであり、過去の操作手順との比較に使える。
- C の「下部固定バー」は、将来ステップ数が増えた場合の有力な選択肢になる。
- いずれも `design-variants.jsx` に隔離してあり、計算・データモデルには影響しない。

通常の利用(`?design` なし)では B が表示され、比較用の切替UIも出ない。

## 6. 再現手順

```bash
npm run build
npm run test:e2e          # 42ケース(アプリ17 + デザイン等価性11 + レスポンシブ14)
```

スクリーンショット比較: `?design=A`、`?design=B`、`?design=C` を同じ案件で開く。
