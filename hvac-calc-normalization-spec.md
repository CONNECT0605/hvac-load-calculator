# 計算エンジン正規化 仕様書(STEP1〜4)

作成日: 2026-09-02
ステータス: 本書はSTEP1〜4(仕様確定)です。**STEP5(実装)はこの後、本書の内容に沿って別途実施します。**

---

## STEP 1｜監査15項目の確定

| No | 現状 | 問題点 | 修正内容 | 計算上の根拠 | 実装方法 | テスト方法 |
|---|---|---|---|---|---|---|
| 1 | 単一の掛け算チェーン(コード765〜768行目) | 中間値が残らず要素分解不可 | 「面積ベース負荷」と「人体負荷」を別変数として保持し合算する構造に変更(§2詳細) | 前回監査レポート§1 | `computeLoad()`関数内で`areaLoadCoolingKW`・`occupantSensibleKW`・`occupantLatentKW`を独立変数として計算・保持 | 各中間値をテスト出力に含め、合計値と突き合わせる |
| 2 | 単位換算は正しい | なし | 変更なし | ― | ― | 既存の単体テストを継続 |
| 3 | 冷房・暖房・換気量とも算出可能だが途中経過なし | 同上 | 中間値をLoadResult相当のオブジェクトで返す | 前回監査レポート§3 | `computeLoad()`が`{areaLoadCoolingKW, occupantSensibleKW, occupantLatentKW, coolingLoadKW, ...}`を返す | 8ケースのテストで中間値を出力・確認 |
| 4 | 一律1.15を無条件適用(766・768行目) | 根拠不明、機種選定側の余裕率と二重計上 | `SAFETY_FACTOR`を計算式から削除。代わりに「計画上の余裕(初期値0%、ユーザー明示設定)」を①計算負荷の後に1回だけ適用し、②機器選定側の余裕率とは完全に分離して表示 | 前回2回の設計書(4段階構造の提案) | `marginPct`(初期値0)を新規state化。`adjustedLoadKW = designLoadKW × (1+marginPct/100)`という形で1箇所のみに適用 | marginPct=0のケースと10%のケースを比較し、機種選定側のsurplusPctが独立して変化しないことを確認 |
| 5 | 要素積み上げ皆無 | 構造体・窓・日射・照明・機器発熱は未実装(今回は対象外、詳細モードで対応) | **今回のスコープ外。** 人体負荷のみ先行して分離実装(§2) | 詳細モード仕様書v1.0(将来対応) | ― | ― |
| 6 | 湿度・外気設計温湿度が未保持 | 潜熱負荷(外気側)は依然計算不能 | **今回のスコープ外。** 人体潜熱のみ簡易式で先行対応(§2) | 同上 | ― | ― |
| 7 | 換気風量のみ実装 | 熱量への変換は未実装 | **今回のスコープ外**(詳細モードで対応) | 同上 | ― | ― |
| 8 | 単一requiredCapacityKWで機種選定、PACKAGE_SIZESに7.1kW(2.5馬力)欠落、実在型式と非接続 | 選定の粒度が粗く、実データと計算が繋がらない | ①PACKAGE_SIZESに7.1kW(63形)を追加 ②`EQUIPMENT_DB`(容量→実在型式配列)を機種選定ロジックに接続し、`hasRealData`フラグと候補型式リストを`recommended`に含める | 前回監査レポート§8 | `EQUIPMENT_DB = {8.0: [...]}`を`recommended`計算内で参照し`recommended.candidateModels`を生成 | 8.0kWが選定されるケースで候補型式が5件表示されること、それ以外の容量では「要確認」表示になることを確認 |
| 9 | 「なぜこの容量か」を説明する変数がない | 追跡不可能 | `selectionReason`という説明文字列を計算結果に含める(§3で詳細設計) | 前回監査レポート§9 | `buildSelectionReason(basis, requiredCapacityKW, recommended)`関数を新設 | 冷房支配ケースと暖房支配ケースでreasonの文言が異なることを確認 |
| 10 | 型式データのみ出典あり、係数は出典なし | 非対称 | 主要係数(原単位・地域係数・人体発熱)にコード内コメントで出典区分(「本アプリの仮定」/「実務解説記事」/「設計基準」)を明記 | 前回監査レポート§10 | 定数定義の直前にJSDocコメントで出典を記載 | コードレビューで全定数に出典コメントがあることを目視確認 |
| 11 | 出典・版・確認日のメタデータなし | 改訂追跡不可能 | **今回は構造化メタデータの導入までは行わず、コメントでの出典明記に留める(スコープを限定)** | ― | 上記10と同様 | ― |
| 12 | ハードコード、変更履歴なし | 改訂時に更新困難 | **今回のスコープ外**(設定外出しは将来対応) | ― | ― | ― |
| 13 | モード分離未実装 | ― | **今回のスコープ外**(詳細モードは別工程) | ― | ― | ― |
| 14 | floorAreaが階数によらず1回のみ使用、ラベルが曖昧 | テストケースの意図とズレる可能性 | 入力ラベルを「延床面積(合計・m²)」に変更し、階数は「機種の階別配分表示にのみ使用する」ことを画面上に注記。計算式自体は変更しない(面積は既に合計値として1回のみ使用する、という解釈を明示的に確定する) | 前回監査レポート§14 | ラベル文言変更+説明テキスト追加(UIの大幅変更ではなくラベル・注記レベル) | 100m²・5階のテストケースで、ラベルが「延床面積(合計)」と表示され、5階が負荷計算に影響しないことを一貫して確認 |
| 15 | 0除算リスク、出典欠如、要素分解不可 | 第三者検証性が限定的 | 0除算ガード(§4)、中間値の保持(No.1と同じ対応) | 前回監査レポート§15 | `requiredCapacityKW<=0`の場合、`surplusPct`計算をスキップし`status:"invalid"`を返す | 「0㎡」「0人+0㎡」ケースでエラーではなく明示的な状態表示になることを確認 |

**今回のスコープ**:ご指示の5点(人体負荷・面積定義明確化・実在機器接続・二重計上排除・異常値処理)に対応するNo.1・4・8・9・14・15を実装対象とし、No.5・6・7・11・12・13は詳細モード実装時までスコープ外とします(前回までの設計書に沿った切り分けです)。

---

## STEP 2｜計算仕様の確定

### 2-1. 全体フロー(決定論的な計算エンジン)

```
入力値(floorAreaTotal, floors, occupants, buildingTypeId, regionId,
       coolingSetTemp, heatingSetTemp, marginPct)
   ↓ 入力検証(§2-4)
   ↓
① 面積ベース負荷(冷房・暖房)
② 人体負荷(顕熱・潜熱、冷房のみ)
③ 計算負荷(①+②) ← 安全率なし
④ 計画上の余裕を適用(marginPct、初期値0%)
⑤ 選定基準容量(冷房・暖房のうち大きい方、どちらが基準かを記録)
⑥ 機種選定(標準容量への当てはめ、実在型式データの接続)
⑦ 選定余裕率(⑥の結果、表示のみ)
⑧ 選定理由テキストの生成
```

同じ入力値を与えれば必ず同じ結果になる純粋関数として実装します(乱数・現在時刻・外部APIへの依存なし)。

### 2-2. 各計算の詳細

#### ① 面積ベース負荷

| 項目 | 値 |
|---|---|
| 入力値 | `floorAreaTotal`(m²)、`buildingType.coolingWm2`/`heatingWm2`(W/m²)、`region.coolingFactor`/`heatingFactor`(無次元)、`tempFactorCooling`/`tempFactorHeating`(無次元) |
| 単位 | W/m² × m² = W → ÷1000 → kW |
| 計算式 | `areaLoadCoolingKW = floorAreaTotal × coolingWm2 × region.coolingFactor × tempFactorCooling / 1000` |
| 係数 | 用途別原単位(例:飲食店244/174 W/m²)、地域係数(関東=1.0基準)、温度設定補正(1±3%/℃) |
| 係数の出典 | **いずれも「本アプリの概算仮定」。外部の公的基準には基づいていません。** これは変更しません(監査で維持と判断済み)。 |
| 適用条件 | 全ケース共通 |
| 中間計算値 | `areaLoadCoolingKW`、`areaLoadHeatingKW` |

#### ② 人体負荷(顕熱・潜熱、冷房のみ)

| 項目 | 値 |
|---|---|
| 入力値 | `occupants`(人) |
| 単位 | W/人 × 人 = W → ÷1000 → kW |
| 計算式 | `occupantSensibleKW = occupants × 60 / 1000`<br>`occupantLatentKW = occupants × 50 / 1000` |
| 係数 | 顕熱60W/人・潜熱50W/人 |
| 係数の出典 | **実務解説記事(株式会社パラダイム)の「事務作業時」の目安値を暫定使用。公的基準ではありません。** 飲食店の接客・調理作業に特化した値ではないため「要確認」タグを付けて表示します。 |
| 適用条件 | 冷房負荷にのみ加算。暖房負荷には加算しません(建築設備設計基準が暖房負荷に内部発熱を原則含めないという考え方に整合させるため) |
| 中間計算値 | `occupantSensibleKW`、`occupantLatentKW` |
| **既知の制約** | ①の面積ベース原単位(244 W/m²等)が人体発熱を含んだ値なのか含まない値なのか、出典を確認できていません。そのため人体負荷を別項目として加算することで、理論上は二重計上のリスクがゼロではありません。今回は監査で指摘された「人体発熱が計算から完全に欠落している」という欠陥の解消を優先し、この制約は明示した上で残す判断とします。詳細モード実装時(原単位法を廃止し積み上げ計算に置き換える時点)に解消されます。 |

#### ③ 計算負荷(設計負荷)

```
designLoadCoolingKW = areaLoadCoolingKW + occupantSensibleKW + occupantLatentKW
designLoadHeatingKW = areaLoadHeatingKW
```
安全率は一切適用しません。

#### ④ 計画上の余裕(旧SAFETY_FACTORの置き換え)

| 項目 | 値 |
|---|---|
| 入力値 | `marginPct`(%、ユーザー入力) |
| 初期値 | **0%**(旧1.15相当の自動適用は廃止) |
| 計算式 | `adjustedCoolingKW = designLoadCoolingKW × (1 + marginPct/100)`<br>`adjustedHeatingKW = designLoadHeatingKW × (1 + marginPct/100)` |
| 適用条件 | ユーザーが明示的に数値を入れた場合のみ効果を持つ。既定は0%=無補正 |
| 位置づけ | これは「安全率」ではなく「発注者・設計者が意図的に見込む計画上の余裕」であり、必ずユーザーが値を選ぶ操作を介します。自動では掛かりません |

#### ⑤ 選定基準容量

```
requiredCapacityKW = max(adjustedCoolingKW, adjustedHeatingKW)
basis = adjustedCoolingKW >= adjustedHeatingKW ? "cooling" : "heating"
```
`basis`(どちらが支配的だったか)を結果に保持し、UIおよび選定理由テキストに表示します(監査No.9への対応)。

#### ⑥ 機種選定・実在型式データ接続

```
PACKAGE_SIZES = [5.6, 7.1, 8.0, 11.2, 14.0, 16.0, 22.4] kW  // 7.1kW(2.5馬力/63形)を追加

各サイズについて:
  count = ceil(requiredCapacityKW / size)
  installedKW = count × size
  surplusPct = (installedKW − requiredCapacityKW) / requiredCapacityKW × 100
  hasRealData = EQUIPMENT_DB[size] が存在するか
  candidateModels = EQUIPMENT_DB[size] || []

recommended = surplusPctが最小のもの(要件を満たすもののみが対象)
```

`EQUIPMENT_DB`は現時点で8.0kWクラスのみ実データ(5社の実在型式)を保持しています。他の容量クラスは`hasRealData:false`となり、UIでは「要確認」と表示されます(データを偽装しません)。

#### ⑦ 選定余裕率

`recommended.surplusPct`をそのまま表示。安全率(④)とは完全に別の数値として画面上でも別項目に表示します。

#### ⑧ 選定理由テキスト

`basis`・`requiredCapacityKW`・`recommended`から、以下のようなテンプレートで文章を自動生成します(STEP3で詳細設計)。

### 2-3. 不足情報の扱い

| ケース | 扱い |
|---|---|
| 建物用途未選択 | 発生しない(セレクト式で既定値あり) |
| 面積が0以下 | 「要確認:面積が0以下のため計算できません」と表示し、負荷・機種選定とも計算しない(0として計算しない) |
| 人数が0 | 有効な入力として計算する(人体負荷0、換気量0)が、「在室人数0人のため換気量が0m³/hです。用途によっては最低換気量の確保が必要な場合があります」と警告表示 |
| 温度設定が極端(例:設定温度差が大きく温度補正係数がマイナスになる) | 温度補正係数がマイナスになった場合、「温度設定補正が異常な値です。入力値を確認してください」と警告し、計算は行うが結果に警告タグを付ける |

### 2-4. 入力検証ルール

| 入力 | 検証 |
|---|---|
| floorAreaTotal | 数値かつ >0。以下の場合は「要確認」ステータス:≤0、または極端に大きい(>50,000m²、飲食店等の用途に対し非現実的な規模として警告のみ、計算は継続) |
| floors | 数値かつ ≥1(既存どおり`Math.max(1, ...)`でクランプ) |
| occupants | 数値かつ ≥0(負数は0にクランプ) |
| coolingSetTemp / heatingSetTemp | 数値。温度補正係数がマイナスになる場合は警告 |
| marginPct | 数値かつ ≥0(負の余裕=削減は許可しない。0以上のみ) |

---

## STEP 3｜機器選定ロジック(追跡可能な設計)

### 3-1. パイプライン

```
必要負荷(designLoadCoolingKW, designLoadHeatingKW)
  ↓
必要容量(requiredCapacityKW, basis)          … STEP2 ④⑤
  ↓
候補機種(PACKAGE_SIZESの全サイズについてcount/installedKW/surplusPctを算出)
  ↓
選定台数(各候補のcount)
  ↓
設置容量(各候補のinstalledKW)
  ↓
余裕率(各候補のsurplusPct)
  ↓
実在型式接続(hasRealData, candidateModels)
  ↓
推奨(surplusPct最小のものをrecommendedとして選択)
  ↓
選定理由(basis・requiredCapacityKW・recommendedから自動生成)
```

各段階の値をすべて`LoadResult`相当のオブジェクトに保持し、UIの「計算根拠を表示」相当の場所でSTEP順に表示することで、SE・設備設計者が追跡できるようにします。

### 3-2. 選定理由テキストのテンプレート(案)

```
"{basis === "cooling" ? "冷房" : "暖房"}負荷が支配的なため、{basis}能力を基準に選定しました。
必要容量は{requiredCapacityKW.toFixed(1)}kWです。
標準機種{recommended.size}kW({recommended.count}台、設置合計{recommended.installedKW.toFixed(1)}kW)は、
候補の中で余裕率が最小(+{recommended.surplusPct.toFixed(1)}%)のため推奨しています。
{recommended.hasRealData ? `この容量クラスは${recommended.candidateModels.length}社の実在型式を確認済みです。` : "この容量クラスの実在型式は未調査です(要確認)。"}"
```

これにより「8.0kW×4台」という結果だけでなく、「なぜ8.0kWなのか(必要容量28.1kWに対し余裕率が最小)」「なぜ冷房基準なのか(冷房負荷が暖房負荷を上回った)」まで文章として追跡可能になります。

---

## STEP 4｜テスト計画

### 4-1. テストケース一覧

| No | ケース | 入力 | 期待される挙動 |
|---|---|---|---|
| 1 | 通常ケース(現行テスト条件) | 飲食店・100m²・30人・関東・26℃/22℃・margin0% | 現行の28.1kW近辺の値が人体負荷加算により変化することを確認(具体的な数値は§4-2で計算) |
| 2 | 0人 | 上記条件で occupants=0 | 人体負荷0、換気量0、警告表示あり。計算自体はエラーにならない |
| 3 | 極端に大きい面積 | floorAreaTotal=100,000 | 警告表示ありだが計算は完了し、機種台数が現実的でない大きな数字になることを許容表示 |
| 4 | 0㎡ | floorAreaTotal=0 | 「要確認」ステータスとなり負荷計算・機種選定を行わない |
| 5 | 複数階 | floors=10、floorAreaTotal=100(据え置き) | 5階のケースと計算結果(kW)が同一であることを確認(floorsが負荷計算に影響しないことの意図的な確認) |
| 6 | 入力不足(空欄) | coolingSetTemp未入力(空文字) | 既定値へのフォールバックまたは警告表示。NaNが結果に伝播しないことを確認 |
| 7 | 機器容量不足に近いケース | 極端に大きい必要容量に対し最大機種22.4kWで多数台数が必要になるケース | countが正しく切り上げ計算され、installedKWがrequiredCapacityKW以上になることを確認 |
| 8 | 必要負荷と機器容量が完全一致 | requiredCapacityKWがちょうど8.0kWの倍数になるよう入力を調整 | surplusPct = 0%となり、0除算やマイナス表示が発生しないことを確認 |

### 4-2. テスト方法

計算ロジックをReactコンポーネントから独立した純粋関数(`computeLoad()`等)として実装し、Node.js上で直接実行するテストスクリプトを用意します。これにより、ブラウザでの手動確認に頼らず、8ケースを自動実行して中間値まで出力・検証します(STEP5で実施し、実行結果を報告します)。

---

以上がSTEP1〜4の確定内容です。この内容に基づき、STEP5(実装)に進みます。
