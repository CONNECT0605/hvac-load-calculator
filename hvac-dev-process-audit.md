# 空調設備施工積算アプリ 開発工程監査レポート

作成日: 2026-09-02
監査方法: 実際のファイル(`hvac-load-calculator.jsx`、`hvac-calc-engine.js`、`hvac-calc-engine.test.js`、既存の設計書4件)をgrep・view・実行結果の記録に基づいて確認。推測は含みません。

---

## 冒頭:最も重要な発見

**現在、「実際に動くアプリ(jsx)」と「最新の計算ロジック(engine.js v3)」が一致していません。** 前回のセッションで、人体負荷の二重計上を避けるための仕様変更(概算負荷と設計用必要負荷の分離、実在機器の正式選定/容量クラス仮選定の区別)を`hvac-calc-engine.js`に実装しましたが、**この変更は`hvac-load-calculator.jsx`(ユーザーが実際に操作する画面)には反映されていません。** さらに、この`engine.js`の新ロジックは一度もテスト実行されていません(最後にテストを実行したのは、これより古いバージョンのロジックに対してです)。

この監査は、この事実を前提に評価しています。

---

## 11工程の評価

| 工程 | 判定 | 根拠 |
|---|---|---|
| 1. 企画・要求整理 | **進行中** | 当初「営業プレゼン用デモ」として開始しましたが、直近の指示では「実務で人に使わせることを前提」と要求水準が変化しています。この目的の変化自体がまだ明文化・合意されていません |
| 2. 要件定義 | **未着手** | 「何を実装すれば初回ローンチ可能か」を定めた要件一覧・受入基準が文書化されていません(本監査が実質的に最初の整理です) |
| 3. 基本設計 | **進行中** | 画面構成(6画面のフロー)はコード上に存在しますが、電気料金計算等、要求されている一部機能は基本設計段階にも達していません |
| 4. 詳細設計 | **進行中** | 計算ロジックについては3本の設計書(計算式監査・詳細モード仕様書v1.0・正規化仕様書)を作成済み。ただし外皮・窓・日射等の詳細負荷計算は設計のみで未実装 |
| 5. 実装・開発 | **進行中** | UIの6画面は実装済み。ただし前述のとおり最新の計算ロジック(engine.js v3)が未反映という重大な手戻りが存在 |
| 6. 単体テスト | **進行中** | engine.jsの旧バージョンに対して8ケードのテストを実行し記録は残っていますが、**現在のengine.js(v3)は一度もテスト実行されていません** |
| 7. 結合テスト | **未着手** | UI(jsx)とロジック(engine.js)を接続した状態でのテストは一度も実施していません(そもそも両者が同期していません) |
| 8. システムテスト | **未着手** | ブラウザでの実機能確認は、本セッションの環境では実施できていません |
| 9. 受入テスト | **未着手** | 実際の設備設計者・営業担当者によるレビューは行われていません |
| 10. リリース・公開準備 | **未着手** | ― |
| 11. 運用・保守 | **未着手** | ― |

**総合評価:詳細設計〜実装〜単体テストの間を行き来している段階であり、結合テスト以降には一度も到達していません。**

---

## A. 現在地

- **進捗工程**:実装・単体テストの境界(ただし直近の変更が未統合・未テストのため、実質的には「実装が最新設計に追いついていない」状態)
- **全体完成度**:目的によって大きく変わります。
  - 「営業プレゼン用デモ」としての完成度:おおむね60〜70%(画面遷移・見た目は成立している)
  - 「実務で人に使わせる計算ツール」としての完成度:**15〜25%程度**(計算の正確性・検証・実データの根幹部分が未確定・未検証のため)
- **実際に動く機能**:6画面のフロー(負荷計算→機器選定→価格比較→工事費概算→原価・利益率→客先向け見積)、標準機種容量への切り上げ計算、サンプル価格による最安/納期優先/バランス比較ロジック、粗利率からの販売価格逆算(`原価÷(1−粗利率)`という正しい式)
- **仮実装・ダミー・未完成の部分**:
  - 価格・納期・在庫データ(すべて明示的なサンプル)
  - 工事費(施工費・電気工事費・配管工事費)の単価はすべて仮定値
  - 実在機器データは8.0kW・5.6kWクラスのみ(残り5クラスは未調査)
  - 電気使用量・電気料金計算(**コード上どこにも存在しません**)
  - engine.js v3で設計した「人体負荷の二重計上回避」「概算負荷/設計用必要負荷の分離」「実在機器の正式選定/仮選定の区別」は、**jsx側に未反映**

---

## B. 空調積算の中核機能の状況

| 機能 | 状態 | 実装内容 |
|---|---|---|
| 面積・寸法入力 | 実装済み(簡易) | 延床面積合計の1値のみ。1フロア面積・対象階別入力は未実装 |
| 建物用途 | 実装済み | 8区分。原単位(W/m²)は出典未確認の仮定値 |
| 階数 | 入力欄のみ | 負荷計算には使用されず、機種の階別配分表示にのみ使用(意図的な仕様。ラベルで明示済み) |
| 窓・開口部 | **未実装** | 入力項目自体が存在しない |
| 方位・向き | **未実装** | 同上 |
| その他設計条件 | 一部実装 | 室内設定温度(冷房・暖房)のみ。湿度・気密性・断熱仕様は未実装 |
| 冷房負荷計算 | 実装済みだが要検証 | 面積原単位法。jsx側は人体負荷を無条件加算(旧ロジック)、engine.js側は条件付き加算に変更済み・**未統合** |
| 暖房負荷計算 | 実装済み | 内部発熱を含めない設計。この点はjsx・engine.jsとも一致 |
| 必要能力計算 | 実装済みだが要検証 | 冷暖房のうち大きい方。上記の不整合の影響を受ける |
| 空調機台数計算 | 実装済み | 切り上げ除算、0除算ガードあり(jsx・engine.js双方でテスト済み) |
| 推奨能力・機種選定 | 実装済みだが不完全 | jsx側は「実データ有無」のみの区別。engine.js側で設計した「正式選定/仮選定」の明確な区別は未統合 |
| 型番データ | 部分実装 | 5.6kW・8.0kWクラスのみ実在型式(各5社)を確認済み。7.1/11.2/14.0/16.0/22.4kWクラスは未調査 |
| 価格・仕入先情報 | **サンプルのみ** | 実売価格は一切取得していません。明示的に「仮データ」表示 |
| 電気使用量・電気料金 | **未着手** | 機能自体が存在しません |
| 結果表示 | 実装済みだが不完全 | 数値は表示されるが、engine.js v3の区分(概算負荷/設計用必要負荷/容量パッケージ/実在機器正式選定)がまだ画面に反映されていません |

---

## C. 計算精度(最重要)

「たぶん大丈夫」は使わず、確認できたことと未検証のことを分けて記載します。

| 観点 | 状況 |
|---|---|
| 計算式の妥当性 | 面積原単位法(244W/m²等)は出典を確認できておらず「本アプリの概算仮定」のまま。人体発熱との二重計上リスクは**未解消**(engine.js側で回避策は設計したがjsxに未統合) |
| 単位 | kW・m²・m³/h・%の換算はコードレビューで確認済み。engine.js側のテストでも単位の破綻は検出されていません |
| 丸め処理 | 表示上の`toFixed(1)`等は確認済み。**内部計算での丸め誤差の蓄積は未検証** |
| 境界値 | engine.js側で「0㎡」「必要負荷=機種容量完全一致」等をテスト済み。**jsx側(実際にユーザーが操作する画面)では同等の境界値テストを実施していません** |
| 入力ミス | 負数のクランプ、NaNのフォールバックは一部実装。**全入力項目を網羅した検証は未実施** |
| 異常値 | 極端に大きい面積(>50,000m²)は警告表示のみで計算続行。この閾値設定自体の妥当性は**未検証** |
| 入力漏れ | 空欄時のフォールバック値(既定温度26/22℃等)はあるが、「本アプリの推測値である」ことの画面上の明示は一部の項目に留まる |
| 計算結果の再現性 | engine.js単体では決定論的な純粋関数として設計・確認済み。**しかしjsxとengine.jsが同期していないため、「画面に表示される数値」と「テストで検証した数値」が現時点で一致する保証がありません。これが現状最大のリスクです** |

---

## D. テスト状況

- **実施済み**:`hvac-calc-engine.js`の旧バージョン(v2相当)に対する8ケース(通常・0人・極端な面積・0㎡・複数階・入力不足・大容量・容量完全一致)の自動実行。結果は記録済みで、期待どおりの挙動を確認しています。
- **未実施**:
  - `hvac-calc-engine.js`の現行版(v3)に対するテストの実行(一度も行っていません)
  - 決定性の確認(同じ入力→同じ結果になることの明示的なテスト)
  - 「1フロア面積×階数」「対象階のみ」といった面積定義のバリエーションテスト
  - 実在機器データあり/なしのケースでの`selectionType`(正式選定/仮選定)の分岐テスト
  - jsx側(実際のUI)に対する動作確認(本セッションの環境ではブラウザを実行できないため、原理的に実施不可能。人力での画面操作確認も未実施)
  - 基準値(手計算や外部の熱負荷計算ツール)との照合
  - 複数現場パターン(異なる用途・規模・地域の組み合わせ)を一括で回すテストスイート

**複数パターンのテストケースを作れる状態か**:engine.js側はテスト可能な構造(純粋関数)になっているため、可能です。**基準値と照合できる状態か**:外部の基準値そのものを保有していないため、現時点ではできません。

---

## E. UI/UX

**前提として、本セッションはブラウザ実行環境を持たないため、以下はコードレビューに基づく評価であり、実機での動作確認はしていません(未検証)。**

| 観点 | 評価 |
|---|---|
| 初めて使う人でも操作できるか | 画面遷移・ラベル・単位表示は整理されているが、「要確認」「仮データ」タグが多く、業務未経験者には情報過多に見える可能性(未検証) |
| 入力から結果まで迷わないか | パンくずナビあり、6画面のリニアフロー。ただし全画面を通しての操作フローそのものは未検証 |
| iPhoneでの使いやすさ | `flex-col md:flex-row`等のレスポンシブクラスは使用されているが、実機のiPhone Safariでの見た目・操作性は**完全に未検証** |
| 結果の見やすさ | 数値は大きく色分け表示。価格比較・見積画面はテーブル列数が多く、モバイルでの横スクロールの見やすさは未検証 |
| 業務用商品としての見た目 | 罫線ベースの落ち着いたデザイン方向性自体は妥当。ただし「仮データ」「要確認」の注記が随所にあり、これは開発中の誠実な表示である一方、**製品として公開する際は情報設計の整理が必要**(注記を隠すのではなく、目立たせ方を変える必要がある) |

---

## F. 初回ローンチの判断

**「写真入力」「図面画像解析」「音声入力」は、初回ローンチに不要です。** コード上どこにも存在せず、着手もしていません。これらは計算精度・基本機能が固まった後の追加機能として明確に切り離すべきです。**今は機能を増やす段階ではありません。**

初回ローンチの完成基準(「計算が正確で、誰でも使えて、実務の負担を削減でき、商品として恥ずかしくない」)に照らすと、**最も未達なのは「計算が正確」の部分**です。UIの骨格や画面遷移はすでに一定水準にありますが、計算ロジックの正確性・検証・実データの根幹がまだ確定していません。

---

## まとめ

1. **今どこまで完成しているか**:UIの流れとしては6画面が動くプロトタイプ段階。計算ロジックは詳細設計〜実装〜単体テストの間を行き来しており、**直近の改善(engine.js v3)が実際のアプリに反映されないまま次の指示に進んでしまった状態**。
2. **初回ローンチまで残っていること**:(a) engine.js v3をjsxに統合し再テスト (b) 電気使用量・電気料金計算の新規実装 (c) 残り5容量クラスの実在機器データ調査 (d) 実売価格データの取得方針決定(現状ゼロ) (e) 実機(iPhone等)でのUI検証 (f) 実際の設備設計者によるレビュー。
3. **計算面で危険な箇所**:最大の危険は「ユーザーが画面で見る数値」と「これまで監査・テストしてきた数値」が一致する保証がないことです。次点は、面積原単位(244W/m²等)の出典不明と、人体発熱の二重計上リスクが実装上まだ解消されていないことです。
4. **テストで確認すべきこと**:engine.js v3を実際に実行し8+αケードの結果を確認すること、そのうえでjsx側で同じ入力を手動確認し数値が一致することを確認すること。
5. **今日優先して実装・検証すべきこと**:新機能の追加ではなく、**engine.js v3をjsxに統合し、両者の数値が一致することをテストで証明すること**。これが完了するまで、他のいかなる機能拡張よりも優先すべきです。
6. **初回ローンチを妨げている最大の問題**:計算ロジックの正確性そのものがまだ確定・検証されていない状態のまま、指示のたびに新しい仕様変更・機能追加を重ねてきたことです。今は仕様追加を止め、計算エンジン1つを実装からテストまで完全に固め切ることが最優先です。

コードの変更は行っていません。次の実装に進む前に、この監査結果についてご確認をお願いします。
