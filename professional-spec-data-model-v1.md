# プロ仕様化 正式データモデル・実装前仕様書

作成日: 2026-09-04
方針: コード変更なし。244W/m²・`status: "provisional"`・既存42/42テストは末尾で変更なきことを確認する。
情報源の区別: 【STABRO公式確認】(本セッションで実際に確認した代理店公式ページの記載)/【STABRO旧版マニュアル等】(今回未確認、将来要調査)/【推測】(未確認)/【ユーザー提供情報】(本指示で共有されたが本セッションでは一次資料未確認の情報)/【国交省基準確認済み】(過去セッションでmlit.go.jp原文確認済み)

**重要な訂正**:「STABROは木造建築物の熱負荷計算に対応していない」という点は、今回のご指示で共有された情報であり、**本セッションでは一次資料(STABRO公式サイト・マニュアル)での確認を行っていません。** 【ユーザー提供情報】として扱い、以降そのまま断定はせず「未検証」と明記します。

---

# A. プロ仕様化に必要な正式データモデル

```
Project(物件)
  └─ Room[](室、1物件に複数)
       ├─ Envelope(外皮・構造体、方位別の壁・屋根・床)
       ├─ Window[](窓・ガラス、方位別)
       ├─ InternalHeat(内部発熱:人体・照明・機器・その他)
       ├─ OutdoorAirVentilation(外気・換気・すきま風)
       └─ RoomDesignCondition(室内設計条件:温湿度・運転時間)
  └─ ProjectDesignCondition(設計条件:地域外気条件・季節条件)
  └─ HourlyLoad[](Room×時刻の負荷、時刻別計算の中間生成物)
  └─ MaxLoad(Room別最大負荷、顕熱・潜熱・発生時刻)
  └─ LoadSummary(物件全体の負荷集計、系統別集計)
  └─ EquipmentSelection(必要能力→機器候補→適正/不足/過大判定)
```

**現行エンジンとの根本的な違い**:現行は`Project`と`Room`が未分離で、建物全体を単一の面積・単一の用途として扱っています。プロ仕様化の第一歩は、この階層を成立させることです(前回のSTABRO比較レポートで既に指摘済み)。

---

# B. 必須入力項目一覧

## B-1. 物件(Project)

| 項目 | 単位 | 必須/任意 | 用途分岐 |
|---|---|---|---|
| 建物用途 | 区分選択 | 必須 | 全用途共通(値は用途ごとに変わる) |
| 所在地(地域/地区) | 区分選択 | 必須 | 全用途共通 |
| 階数 | 階 | 必須(表示・室配置用) | 全用途共通 |
| 対象階 | 階番号 | 任意(未指定なら全階対象) | 全用途共通 |
| 延床面積 | m² | 必須 | 全用途共通 |
| 空調対象面積 | m² | 任意(未指定なら延床面積を使用) | 全用途共通 |
| 使用時間帯 | 時刻範囲 | 任意(標準値あり) | 用途により標準値が異なる(飲食店は夜間営業等) |

## B-2. 室(Room)

| 項目 | 単位 | 必須/任意 | 備考 |
|---|---|---|---|
| 室名 | 文字列 | 必須 | 識別用 |
| 室用途 | 区分選択 | 必須 | 物件全体の用途と異なる室(例:オフィスビル内の会議室)を許容 |
| 面積 | m² | 必須 | |
| 天井高さ | m | 任意(標準値2.6m等) | 室容積の算定に必要 |
| 室容積 | m³ | 自動計算(面積×天井高) | すきま風負荷算定に必要 |
| 室内温度(冷房/暖房) | ℃ | 必須(標準値あり) | |
| 室内湿度(冷房/暖房) | %RH | 任意(標準値あり) | 現行未実装 |
| 在室人数 | 人 | 必須 | |
| 照明 | W/m²または機器積み上げ | 任意(標準値あり) | 現行未実装 |
| 機器発熱 | W/m²または機器積み上げ | 任意(標準値あり) | 現行未実装 |
| 外気量 | m³/h | 自動計算または直接入力 | |
| 換気条件 | 区分選択(全熱交換有無等) | 任意 | |
| 運転時間 | 時刻範囲 | 任意(物件の使用時間帯を継承) | |

## B-3. 外皮・構造体(Envelope)

| 項目 | 単位 | 必須/任意 | 備考 |
|---|---|---|---|
| 外壁・屋根・床・内壁・天井・地中壁・土間床 | 各面積(m²)・方位(外壁のみ) | 詳細計算時は必須、概算時は任意 | 中間階では屋根・土間床は対象外等の判定が必要 |
| 熱貫流率(U値) | W/(m²・K) | 詳細計算時は必須 | 材料構成から算定、またはカタログ値直接入力 |
| 熱容量/熱的特性 | J/(m²・K)等 | 時刻別計算(蓄熱考慮)時のみ必須 | 静的計算では省略可 |
| 方位 | 8方位 | 外壁・窓は必須(日射算定に必要) | 屋根・床は方位概念なし |

## B-4. 窓(Window)

| 項目 | 単位 | 必須/任意 | 備考 |
|---|---|---|---|
| 窓面積 | m²(方位別) | 詳細計算時は必須 | |
| 方位 | 8方位 | 同上 | |
| ガラス種類 | 区分選択 | 詳細計算時は必須 | 単板/複層/Low-E等 |
| 熱貫流特性(U値) | W/(m²・K) | ガラス種類から参照、上書き可 | |
| 日射取得特性(η値/SC値) | 無次元 | 同上 | |
| 日除け/遮蔽条件 | 区分選択(庇・ブラインド・ルーバー等) | 任意(標準値=遮蔽なし) | |

## B-5. 内部発熱(InternalHeat)

| 項目 | 単位 | 必須/任意 | 備考 |
|---|---|---|---|
| 人体顕熱・潜熱 | W/人(作業強度別) | 必須(標準値あり) | 現行実装済みだが根拠未確認 |
| 照明 | W/m²または機器積み上げ | 任意(標準値あり) | 現行未実装 |
| 機器 | W/m²または機器積み上げ | 任意(標準値あり) | 現行未実装 |
| その他発熱 | 機器積み上げ | 任意 | 厨房機器等、用途固有 |
| 使用スケジュール | 時刻別在室率等 | 時刻別計算時のみ必須 | |

## B-6. 外気(OutdoorAirVentilation)

| 項目 | 単位 | 必須/任意 | 備考 |
|---|---|---|---|
| 外気温度・湿度(設計用、季節別) | ℃・%RH | 詳細計算時は必須 | 現行は地域係数のみで実温度なし |
| 外気量 | m³/h | 必須 | 現行実装済み |
| 換気方式 | 区分選択 | 任意 | 全熱交換の有無 |
| すきま風 | サッシ気密性・開口部条件 | 任意(標準値あり) | 基準に算定式あり、現行未実装 |
| 全熱交換器等 | 交換効率 | 任意 | |

## B-7. 設計条件(ProjectDesignCondition)

| 項目 | 単位 | 必須/任意 | 備考 |
|---|---|---|---|
| 冷房設計条件 | 室内外温湿度セット | 必須 | |
| 暖房設計条件 | 同上 | 必須 | |
| 外気設計条件(地域/地区データ) | 都市別データセット | 必須(詳細計算時) | 【STABRO公式確認】全国80地区データを標準搭載という実例あり |
| 設計時刻(冷房・暖房それぞれの代表日) | 日付・時刻 | 時刻別計算時のみ必須 | |
| 日射条件 | 方位別・時刻別日射量 | 時刻別計算時のみ必須 | |

---

# C. 必須計算項目一覧(国交省基準の冷房8項目・暖房5項目との照合)

## 冷房負荷

| 項目 | 今回のアプリで必要か | どの入力から算出するか | 計算式/係数の状況 | 現状実装 |
|---|---|---|---|---|
| 構造体 | **必要**(基準の必須項目) | Envelope(U値・面積)、DesignCondition(ΔT) | U値等の数値係数は未確定(SHASE便覧等が必要) | 未実装 |
| ガラス | **必要** | Window(U値・面積)、DesignCondition(ΔT) | 同上 | 未実装 |
| ガラス日射 | **必要** | Window(面積・SC値)、日射条件(方位別・時刻別) | 日射量データが未確定(EA気象データ等が必要) | 未実装 |
| 照明 | **必要** | InternalHeat(照明W/m²) | 原単位の出典未確認 | 未実装 |
| 人体 | **必要** | InternalHeat(人数・顕熱潜熱W/人) | 60W/50Wは実務解説記事由来、根拠未確認 | 実装済み(既定では設計用必要負荷に不算入という現行独自運用) |
| その他内部発熱 | **必要**(用途依存が強い) | InternalHeat(機器発熱) | 原単位未確定 | 未実装 |
| すきま風 | **必要** | OutdoorAirVentilation(室容積・気密性区分) | 基準に算定式明記(既確認)、係数のみ未実装 | 未実装 |
| 外気 | **必要** | OutdoorAirVentilation(外気量・比エンタルピー差) | 空気物性値(比熱・密度)の採用値要確認 | 部分実装(風量[m³/h]のみ、熱量化なし) |
| ダクト/配管等・漏気・ファン/ポンプ・間欠空調 | **今回は不要と判断**(基準も算定式を明示していない項目) | ― | 基準自体が数値化していない | 未実装・当面対象外 |

## 暖房負荷

| 項目 | 今回のアプリで必要か | どの入力から算出するか | 計算式/係数の状況 | 現状実装 |
|---|---|---|---|---|
| 構造体 | **必要** | 冷房と同一のEnvelopeを暖房用ΔTで再計算 | 同上 | 未実装 |
| ガラス | **必要** | 冷房と同一のWindowを暖房用ΔTで再計算 | 同上 | 未実装 |
| すきま風 | **必要** | 冷房と同一、冬期換気回数を使用 | 基準に算定式明記(既確認) | 未実装 |
| 外気 | **必要** | 冷房と同一、暖房用外気条件で再計算 | 同上 | 部分実装(風量のみ) |
| ダクト/配管等・漏気・間欠空調 | **今回は不要と判断** | ― | 同上 | 未実装・当面対象外 |

**日射・照明・人体・その他内部発熱は暖房には含めない**という前回までの整理(建築設備設計基準の安全側の考え方)を維持します。

---

# D. 必須出力項目一覧

STABRO【公式確認】の帳票構成(表紙・熱負荷計算書・熱負荷集計表・設計条件9帳票・チェックリスト6帳票=計18帳票)を参考に、**第1段階では以下を最小限の出力候補とします**(フル18帳票の再現は将来拡張)。

| 出力項目 | 第1段階で必須か |
|---|---|
| 室別冷房最大負荷(顕熱・潜熱・合計) | 必須 |
| 室別暖房最大負荷 | 必須 |
| 建物全体最大負荷(室の合算) | 必須 |
| 最大負荷発生時刻 | 時刻別計算を実装する場合のみ必須 |
| 系統別最大負荷 | 将来拡張(現行は系統概念なし) |
| 負荷集計表(STABRO相当) | 将来拡張 |
| 必要空調能力・機器候補・適正/不足/過大判定 | 必須(現行の機器選定ロジックを拡張利用) |
| 計算根拠の説明(どの入力・係数から算出したか) | 必須(現行の`loadComponents`構造を拡張利用) |

---

# E. 時刻別計算の仕様(HourlyLoad)

## 対象データ

時刻別外気条件・時刻別日射・時刻別在室人数・時刻別照明・時刻別機器発熱・時刻別外気量、それぞれについて顕熱・潜熱を保持。

## 合算方式の決定

**「同一時刻の負荷を合算し、その時系列の最大値を採用する」方式を正式仕様とします。** 「各項目の最大値を単純に足し合わせる」方式は採用しません(前回までの詳細モード仕様書v1.0・§7で既に確定済みの方針を踏襲)。理由:日射は方位により時刻ごとにピークが異なり、項目別最大値の単純合算は物理的に発生しえない過大な負荷を生む可能性があるためです。

## 最大負荷発生時刻の扱い

室ごとに、時系列の負荷合計が最大となる時刻を記録し、`MaxLoad.peakHour`として保持します。

---

# F. 最大熱負荷の出力候補(確定)

D節と重複するため、ここでは`MaxLoad`データモデルのフィールドとして再掲します。

```
MaxLoad {
  roomId
  cooling: { sensibleKW, latentKW, totalKW, peakHour }
  heating: { sensibleKW, latentKW, totalKW, peakHour }  // 暖房は内部発熱を含まないため顕熱のみが中心
}
LoadSummary {
  projectId
  rooms: MaxLoad[]
  buildingTotalCoolingKW
  buildingTotalHeatingKW
  systemTotals: []  // 系統別集計、将来拡張
}
```

---

# G. 機器選定への接続仕様

```
熱負荷(MaxLoad/LoadSummary)
  ↓
必要能力(requiredCapacityKW、既存ロジック流用)
  ↓
機器能力(EQUIPMENT_DBの実在機種、既存)
  ↓
適正/不足/過大 判定(新規)
```

**適正/不足/過大の判定ロジックは、既存の`surplusPct`(余裕率)を再利用します。** 例:余裕率が0〜20%を「適正」、マイナス(不足)を「不足」、20%超を「過大」とする、といった具体的な閾値は**今回根拠のある基準を確認できていないため、仮の分類ロジックとして新規に数値を決め打ちすることはしません。** 閾値の根拠(STABRO/SeACD等が採用する基準、または国交省基準の記載)が確認でき次第、正式に仕様化します。

**メーカー固有の選定ロジック(ダイキンDK-BIM、三菱MEL-BIM等の内部ロジック)は、公開されていないため一切仮定しません。** 現行の「実在機器と紐づけるが、選定の最終判断は行わない」という設計思想を維持します。

---

# H. 用途別モデルの分離方針

| 用途 | 分岐が必要な理由 | 対応方針 |
|---|---|---|
| オフィス | 標準的な時間帯運転、内部発熱は機器(OA機器)中心 | 標準スケジュール(平日日中)をデフォルトに |
| 店舗・飲食店 | 厨房機器発熱、来客による人数変動、夜間営業等 | 用途別スケジュールテンプレート、厨房発熱の個別入力を用意(D分類=用途限定項目) |
| 住宅・集合住宅 | 就寝時間帯の負荷変動、木造が多い | 木造建築物対応の要否を別途検討(下記) |
| 大型施設(病院・工場等) | 特殊な外気条件・機器発熱、24時間運転等 | 用途別の外気量原単位・スケジュールを個別管理 |

**木造建築物への対応可否**:【ユーザー提供情報】として「STABROは木造建築物の熱負荷計算に現在対応していない」という情報を共有いただきましたが、本セッションではSTABRO公式資料でこれを直接確認できていません。**現行エンジンも木造/RC造/鉄骨造という構造区分自体を持たないため、この論点はデータモデル拡張(Envelopeに構造区分を追加)の際に合わせて検討する必要があります。** 木造特有の熱容量特性(RCより蓄熱が小さい)は、前回のSHASE-S112系調査で確認した「予熱・予冷時間補正係数(RC造1.13、木造・鉄骨造1.33)」とも関連するため、構造区分の実装時に併せて扱うのが合理的です。

---

# 差分整理(5分類)

| 分類 | 項目 |
|---|---|
| **①現在あるもの** | 用途別選択、地域選択、面積・階数・人数入力、温度設定、人体負荷(顕熱潜熱、根拠未確認)、換気量算定、機種選定(実在機器接続、5.6kW/8.0kWクラスのみ)、余裕率表示、244W/m²等の暫定原単位、`calculationMethod`/`loadComponents`によるトレーサビリティ構造 |
| **②追加必須** | Project/Room階層の分離、Envelope(構造体)、Window(窓・方位・日射)、照明・機器発熱の入力、外気設計条件(実温湿度データ)、すきま風負荷(基準の式を実装するだけで対応可能) |
| **③追加推奨** | 時刻別計算(HourlyLoad)、最大負荷発生時刻の記録、負荷集計表(LoadSummary)、適正/不足/過大の機器選定判定 |
| **④用途限定** | 厨房発熱、病院外気条件、工場機器発熱、住宅スケジュール、木造建築物対応 |
| **⑤将来拡張** | 系統別集計、STABRO相当の帳票フル出力(18帳票相当)、メーカーBIM連携(DK-BIM/MEL-BIM相当) |

---

# 第1段階と第2段階以降の切り分け

## F. 第1段階で実装する範囲

1. Project/Roomのデータモデル分離(複数室を扱えるようにする、これが土台)
2. Envelope・Windowの入力構造追加(数値係数は「未確定」表示のまま、構造だけ先行)
3. 照明・機器発熱の入力構造追加(同上)
4. すきま風負荷(基準に算定式が明記済みのため、係数さえ確定すれば実装可能)
5. 外気負荷の熱量化(風量→顕熱・潜熱への変換式は一般的な湿り空気の式のため、空気物性値の採用値さえ確認できれば実装可能)

## G. 第2段階以降に回す範囲

1. 時刻別計算(気象データの調達が前提、最も工数が大きい)
2. 負荷集計表・系統別集計・フル帳票出力
3. 適正/不足/過大の機器選定判定(閾値の根拠確認が前提)
4. 木造建築物対応
5. メーカーBIM連携相当の機能

---

# H'. 244W/m²を正式値へ変更するために必要な根拠

変更なし(前回までと同じ結論)。以下のいずれかが確認できるまで、`status: "provisional"`を維持します。

1. SHASE-S112-2009原本(847円+税、購入可能であることを確認済み)の飲食店舗の基準熱負荷
2. 国交省「建築設備設計基準の資料」(国営設第160号、有償書籍)の該当数値
3. 上記が入手できない場合、Project/Room階層による積み上げ計算が実装された時点で、244という単一係数自体が不要になる(前回結論のとおり)

---

# 最終確認

- **42/42テストが維持されていること**:本レポート作成にあたり実行して確認済み(結果:42件成功・0件失敗)
- **コード変更がないこと**:`hvac-calc-engine.js`・`hvac-load-calculator.jsx`・`hvac-calc-engine.test.js`のいずれも編集していません
- **244W/m²がprovisionalのままであること**:`value: 244`・`status: "provisional"`を実行して確認済み(変更なし)
