# STABRO / SeACD 基準 照合監査レポート（2026-09-15）

方針: **既存COREを破壊しない。一致しているものは触らない。差異があるものだけ最小限で修正する。**
本監査で `hvac-calc-engine.js` および SHARED-LOGIC ブロックは**1行も変更していません**。

```
git diff --stat -- hvac-calc-engine.js hvac-load-calculator.jsx   # → 差分なし
npm run check:sync                                                # → 一致: 283行
```

---

## 0. 基準資料（出典の区別）

| 区分 | 資料 |
|---|---|
| **【一次】** | STABRO負荷計算 公式製品ページ（izmc.co.jp）／公式FAQ／開発元 note 記事／PR TIMES 公式リリース |
| **【一次】** | SeACD 公式製品ページ（izmc.co.jp） |
| **【一次】** | 建築設備設計基準（令和6年版）※リポジトリ内の既存監査で本文確認済みの記載を引用 |
| **【未入手】** | STABRO 操作マニュアル本文、SeACD 操作マニュアル本文、カタログPDF（画像のみでテキスト抽出不可） |

STABRO が準拠する基準は公式に明示されています：計算方法＝「建築設備設計基準 令和6年版」、出力様式＝「建築設備設計計算書作成の手引き 令和6年版」（公式FAQ）。

---

## 1. 現在COREで完成していた機能

- 面積原単位法による**冷房・暖房の最大熱負荷算出**（用途8種 × 地域8種）
- **概算負荷（roughLoad）と設計用必要負荷（designLoad）の分離**。人体負荷は既定で不算入（二重計上回避）
- **温度設定補正**・**余裕率（marginPct）**の適用
- **換気量算出**（在室人数 × 用途別原単位）
- **機器選定**（実在型式／容量クラス仮選定の区別、`selectionType`、根拠テキスト、`source` による追跡）
- **案件 → 階 → 室**の階層データモデル（`project-model.mjs`）
- **室ごとの計算 → 建物全体への合算**（`computeProject`。合算のみで新規の式なし）
- **案件の保存・読込・複製・削除**（localStorage、スキーマ正規化、破損データ耐性）
- **帳票表示・CSV出力（BOM付き）・印刷/PDF**
- **天気表示**（Open-Meteo。外部API遮断時のフォールバックあり）
- 計算エンジンと JSX の同期を機械検証する `check:sync`

---

## 2. STABRO基準と一致していた項目

| 項目 | 状態 |
|---|---|
| 最大熱負荷を算出し機器選定へ引き渡す全体構造 | **一致**（公式確認：SeACD/DK-BIM/MEL-BIM 連携） |
| 室単位で建物を分割して計算する階層 | **一致**（公式確認：室登録画面） |
| 冷暖房を別々に算出し、大きい方を必要容量とする | **一致** |
| 暖房に内部発熱（人体）を算入しない | **一致**（設計基準の安全側の考え方） |
| 室ごとの負荷を即時確認できる | **一致**（公式確認：「室ごとの負荷を即時確認」） |
| SI単位（kW / m³/h / m² / ℃） | **一致**（公式確認：SI単位で計算） |
| 出力に「未実装項目」を明示する誠実表示 | **一致方向**（本アプリ独自の追加。STABROは非該当） |

---

## 3. STABRO基準と差異があった項目（未修正・差異の記録）

計算に効く入力は **用途・面積・在室人数・室内温度** のみです。以下は**UIに入力欄があるが計算に反映されません**（実測で確認、§5参照）。

| 分類 | 項目 | STABRO基準 | 現行CORE | 差異 |
|---|---|---|---|---|
| 設計条件 | 設計用屋外条件（**全国80地区**、冬期夜間温度） | 標準搭載 | 8地域の**無次元係数**のみ。実気温・湿度データなし | **大** |
| 設計条件 | 室内湿度 | 屋内データ登録で温湿度管理 | 入力欄はあるが計算未使用 | **中** |
| 外皮 | 構造体（壁・屋根・床）貫流負荷 | 構造体登録（各100種、SHASE便覧準拠） | 入力可・計算未使用 | **大** |
| 外皮 | 窓ガラス貫流・日射負荷 | 構造体の一種として日射遮蔽まで扱う | 入力可・計算未使用 | **大** |
| 方位 | 方位別の壁面・窓面管理 | 室ごとに構造体を配置 | 8方位を保持・計算未使用 | **大** |
| 日射 | 方位・時刻別日射量 | （ルーバー縦の追加から存在を確認） | 未実装 | **大** |
| 内部発熱 | 照明・機器発熱 | 室入力項目 | 入力可・計算未使用 | **中** |
| 内部発熱 | 人体（顕熱60W/潜熱50W） | 室条件 | 算出するが**既定で不算入** | **中** |
| 外気 | 外気負荷（顕熱・潜熱） | 冷房⑦・暖房④ | 換気量のみ算出、熱量は未算入 | **大** |
| すきま風 | すきま風負荷 | 基準に算定式あり | 未実装 | **中** |
| 時刻 | 時刻別負荷集計・最大値抽出 | 「最大熱負荷計算ソフト」 | 単一時点の静的計算 | **大** |
| 集計 | 熱負荷集計表 | 独立した帳票 | 室の合算は実装、帳票は簡易 | **中** |
| 出力 | 18帳票（計算書・集計表・設計条件9・チェックリスト6）、EXCEL/TSV | 手引き準拠 | 画面表示＋CSV 1種 | **中** |
| 材料 | 構成材料データ（SHASE便覧準拠） | 標準添付 | 未実装 | **大** |
| 地区 | ユーザー気象データ登録・再利用 | 対応 | 未実装 | **小** |

---

## 4. 修正した項目（今回）

| # | 不具合 | 原因 | 修正 | 計算への影響 |
|---|---|---|---|---|
| 1 | **室名を空にすると計算ステップに進めなくなるのに、不足件数0件で理由が表示されない**（無言のブロッカー） | `getStepStatus` の `rooms.done` 判定が `!!r.name` を要求している一方、`issues` には面積未入力しか挙げておらず、室名の欠落が通知されない | `issues` に室名未入力の説明を追加 | **なし**（判定条件は変更せず、通知のみ追加） |
| 2 | **LOAD CORE が固定されていない**（Golden Case・回帰ロックが存在しない） | 数値の凍結手段がなく、将来の変更を検出できない | `core-lock.test.mjs` を追加し `npm test` に組込み | **なし**（テスト追加のみ） |

修正1は「計算できない理由をユーザーが特定できない」実務上の停止要因であり、**UI/UX層の最小修正**です。計算COREには触れていません。

---

## 5. 入力項目が計算に反映されるかの実測（推測ではなく差分で判定）

同一案件に対し各入力を1つずつ変え、冷房・暖房・換気量の変化を測定しました。

```
基準: 冷房 34.4kW / 暖房 24.4kW / 換気 600m³/h
```

| 変更した入力 | 結果 |
|---|---|
| 外壁(南100m², U=0.5)追加 | 変化なし |
| 屋根(200m², U=0.3)追加 | 変化なし |
| 床(200m², U=0.4)追加 | 変化なし |
| 窓(南50m², Low-E, U=2.0, SC=0.4)追加 | 変化なし |
| 窓を西向き100m²へ変更（方位） | 変化なし |
| 照明 15W/m² | 変化なし |
| 機器 20W/m² | 変化なし |
| 室内湿度(冷房50%) | 変化なし |
| 外気量 500m³/h 直接入力 | 変化なし |
| 換気方式=第1種 | 変化なし |
| 熱交換器(効率70%) | 変化なし |
| すきま風(サッシ良好・外部扉あり) | 変化なし |
| 天井高 4.0m | 変化なし |
| 運転時間 9:00-18:00 | 変化なし |

**14件すべてが計算結果に影響しません。** これは「積み上げ方式が未実装」という既知の制約どおりであり、アプリは各画面で
「入力値は記録用で、負荷計算結果には反映されません」と**明示的に告知**しています（画面5・6・7・8、帳票注意事項）。
したがって本件は「隠れた不整合」ではなく、**宣言済みの未実装**と判断し、計算式の追加は行いませんでした（指示#4「推測で新しい計算式・係数を作らない」に従う）。

---

## 6. 計算式を変更した項目

**なし。** 計算式・係数・条件分岐・基準値は一切変更していません。

```
変更前 = 変更後（COREハッシュ 827a1d30c7f576b84087492f86d45bb0cfcbaf8d5329591a73c42a60cd869384）
```

現行式（凍結）:

```
冷房概算 = 面積 × 用途別冷房原単位(W/m²) × 地域係数 × 温度補正 ÷ 1000
暖房概算 = 面積 × 用途別暖房原単位(W/m²) × 地域暖房係数 × 温度補正 ÷ 1000
温度補正(冷房) = 1 + (26 − 冷房設定温度) × 0.03
温度補正(暖房) = 1 + (暖房設定温度 − 22) × 0.03
設計用必要負荷 = 概算負荷 × (1 + 余裕率/100)   ※人体加算ON時のみ冷房に顕熱+潜熱
必要容量 = max(設計用冷房, 設計用暖房)
換気量 = 在室人数 × 用途別原単位(m³/h・人)
```

---

## 7. 変更しなかった項目

- `hvac-calc-engine.js` 全体（**無変更**）
- SHARED-LOGIC ブロック（**一字一句同一・283行**）
- `hvac-load-calculator.jsx`（**無変更**）
- 244W/m² 等の原単位、`REGIONS` の係数、`OCCUPANT_HEAT`、`EQUIPMENT_DB`
- 既存テスト（`hvac-calc-engine.test.js` 等）
- データモデルの構造（`Room`/`Floor`/`Envelope`/`Window`）
- 天気機能（指示#11により監査対象から分離。**UIも挙動も無変更**）

---

## 8. LOAD CORE の現在状態

**固定済み。** `core-lock.test.mjs`（50件）が数値を凍結し、`npm test` で毎回検証されます。

検証内容:
- 代表ケースの冷房/暖房/必要容量/換気量（7ケース）
- 全用途×全地域（8×8=64）が**決定論的**であること
- 条件分岐の凍結: 余裕率×1.15、人体加算ON時の冷房加算・暖房不変、温度単調性、階数が負荷に無関係
- 案件集計の凍結: 室の単純合算、必要容量＝冷房/暖房の大きい方
- 機器選定の凍結: 設置容量≥必要容量、余裕率の定義、選定区分
- **COREハッシュ**（64ケース）: `827a1d30c7f576b84087492f86d45bb0cfcbaf8d5329591a73c42a60cd869384`

数値を変える場合は、変更根拠と「変更前 → 変更後」を明示して `UPDATE_CORE_LOCK=1 node core-lock.test.mjs` で更新する運用です。以降の機能はこのCOREを利用します。

---

## 9. DESIGN MODULE の実装状態

**部分実装（構造は正しい / データは限定的）。**

現在の構造は指示#9が求める形になっています。

```
HVAC CORE (hvac-calc-engine.js = SHARED-LOGIC)
    ↓
LOAD MODULE (project-model.mjs: computeProject / roomToLoadInput)
    ↓
LoadResult (designLoadCoolingKW / designLoadHeatingKW / requiredCapacityKW)
    ↓
DESIGN MODULE (selectEquipment → EQUIPMENT_DB)
```

実装済み: 機器選定（容量クラス・実在型式の区別）、根拠テキスト、`source{name,url,confirmedDate}` による追跡、階別配分（`perFloor`）。
未実装: メーカー機器データベース（国内主要メーカー諸元）、PQ線図・能力補正、系統図、エアフロー図、機器表の自動生成。
→ SeACD相当の機能追加は、**別の計算基盤を作らず**この構造に接続する形で行うのが正しい進め方です（今回は未着手。指示#9は構造の要求であり、計算CORE確定が先行条件のため）。

---

## 10. CAD/BIM の実装状態

**未実装。** CAD/BIMの読込・情報抽出は存在せず、アップロードUIもありません（E2Eで「CAD/BIMのアップロードUIが存在しない」ことを検証済み）。

指示#10が求める構造（CAD/BIM → 情報抽出 → 既存データモデル → 固定済みCORE → LOAD/DESIGN）のうち、
**受け皿となる既存データモデル（Room/Envelope/Window/DesignCondition）は既に存在**します。
したがって将来のCAD/BIM取込は、このモデルへ変換するアダプターとして実装すれば、手入力と同一のCOREを通ります。新しい計算エンジンは不要です。

---

## 11. 天気機能の状態

**無変更（指示#11に従い監査対象から分離）。**

- `weather.mjs`（82行）＋天気パネル。Open-Meteo を利用、API遮断時のフォールバックあり
- **設計用外気条件（負荷計算に使う基準値）はリアルタイム天気APIで置き換えていません**。天気は表示専用で、COREの入力には流れていません（§5の実測で、外気条件の変更が計算に影響しないことと整合）
- 後工程で `Weather Adapter → 現在地・都市・天気情報` として独立モジュール化する方針は維持

---

## 12. UX/UI の完成状態

- **最終デザイン: B（段階誘導型）**。10項目を「つくる/あわせる/だす」の3段に集約し、10項目への直接移動も維持
- A（一望型）/C（集中型）は `?design=A|C` で参照可能（通常利用ではBのみ表示、比較UIは非表示）
- 配色は白・ネイビー・ブルー・ブラック/グレー、UIは日本語、PCは左ナビ＋中央入力＋右ステータス、モバイルは1カラム
- 実測（`design-comparison.md`）: 初期画面の縦長さはA比でPC 1233→1090px、スマホ 2237→1520px。全幅で横スクロール0px
- レスポンシブE2E 14件（320〜1440px）PASS。タップ領域44px以上を検証
- 今回、無言のブロッカー（室名未入力で理由非表示）を修正し、**進めない理由が必ず表示される**状態にしました

---

## 13. 残っている課題

優先度順。

1. **詳細（積み上げ）方式の未実装** — 外皮・窓・日射・内部発熱・外気負荷・すきま風・時刻別計算。§5の14項目。UIは受入済みで、計算式の実装が残っています。実装には基準の係数表（U値・SC・日射量・すきま風量表）と気象データが必要で、**推測で埋めるべきではない**領域です
2. **設計用屋外条件が8地域の無次元係数のみ** — STABROは全国80地区の実データ（冬期夜間温度含む）。基準原本の転記が必要
3. **メーカー機器データベース未搭載** — 国内主要メーカーの諸元、PQ線図、能力補正
4. **帳票が18帳票構成に未対応** — 熱負荷集計表・設計条件9帳票・チェックリスト6帳票、EXCEL/TSV出力
5. **材料データ（SHASE便覧準拠）未搭載**
6. **CAD/BIM取込アダプター未実装**（受入モデルは存在）
7. **湿度・運転時間が計算未使用** — 詳細方式の実装時に同時対応
8. 未保存入力はリロードで失われる（要求仕様に自動保存の記載がないため明示保存方式）

---

## 14. テスト結果（本監査後）

```
npm test           → 57件成功 / 0件失敗（既存）
                   + LOAD CORE ロック 50件成功 / 0件失敗（新規）
npm run check:sync → 一致: 283行
npm run build      → 成功
npm run test:e2e   → 42 passed
```

COREハッシュは監査前後で不変（`827a1d30…`）。
