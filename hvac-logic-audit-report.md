# 現行計算・機器選定ロジック 実務精査監査レポート

作成日: 2026-09-02
対象: `/mnt/user-data/outputs/hvac-load-calculator.jsx`(現在デプロイされているコード。**本レポートによるコード変更は行っていません**)
位置づけ: 前回までの2つの設計書(計算式監査・詳細モード計算仕様書v1.0)を踏まえ、**実装済みコードそのもの**を行単位で精査した結果です。設計書は「あるべき姿」、本レポートは「今動いているものの実態」を扱います。

---

## 総括:現状の信頼できる範囲

**現行コードは「初期計画段階のスケール感をつかむための概算ツール」の水準に留まっており、実施設計・見積確定の根拠としては使用できません。** 具体的には:

- 信頼できる部分:換気量計算(人数×原単位)、機器の台数計算(切り上げ除算)、余裕率の算術自体は式として正しく動いています。
- 信頼できない部分:冷房・暖房負荷の値そのもの(構造体・窓・日射・人体・照明・機器発熱・すきま風・外気を個別に積み上げていない)、温度設定補正(根拠不明の独自係数)、安全率1.15(根拠不明・二重掛けの疑い)、機種選定と実在機種(REAL_EQUIPMENT_80)が計算上ずっと接続されていない点。

以下、ご指示の15項目を順に監査します。

---

## 1. 現在使用しているすべての計算式(コード上の実体)

`hvac-load-calculator.jsx` 757〜796行目の`useMemo`が計算の中心です。

```javascript
tempFactorCooling = 1 + (26 - coolingSetTemp) * 0.03
tempFactorHeating = 1 + (heatingSetTemp - 22) * 0.03

coolingLoadKW = area × buildingType.coolingWm2 × region.coolingFactor × tempFactorCooling × 1.15 / 1000
heatingLoadKW = area × buildingType.heatingWm2 × region.heatingFactor × tempFactorHeating × 1.15 / 1000

ventilationM3h = occupants × buildingType.ventPerPerson

requiredCapacityKW = max(coolingLoadKW, heatingLoadKW)

// 機種ごとに(775〜781行目)
count = ceil(requiredCapacityKW / p.kw)
installedKW = count × p.kw
surplusPct = (installedKW − requiredCapacityKW) / requiredCapacityKW × 100

// 推奨機種(783行目)
recommended = surplusPctが最小のもの
```

これが現行コードの計算式の**すべて**です。負荷を構成する個別要素(構造体・窓・日射・人体・照明・機器発熱・すきま風・外気)を計算する式はコード上に**一つも存在しません**。

---

## 2. すべての入力値・出力値・中間値の単位

| 変数 | 単位 | 妥当性 |
|---|---|---|
| `floorArea` | m² | 入力側の単位表記は正しい |
| `buildingType.coolingWm2` / `heatingWm2` | W/m² | 正しい |
| `region.coolingFactor` / `heatingFactor` | 無次元 | 正しい(が中身は§6で問題視) |
| `tempFactorCooling` / `tempFactorHeating` | 無次元 | 正しい(が中身は§4で問題視) |
| `SAFETY_FACTOR` | 無次元 | 正しい(が中身は§4で問題視) |
| `coolingLoadKW` / `heatingLoadKW` | kW(W/m²×m²=W、÷1000でkW) | **単位換算は正しい** |
| `ventilationM3h` | m³/h(人×m³/h/人) | **単位換算は正しい** |
| `p.kw`(PACKAGE_SIZES) | kW | 正しい |
| `surplusPct` | %(無次元比×100) | 正しいが、後述のとおり分母が0になるケースで破綻(§15) |

**単位換算自体に誤りはありません。** 問題は単位ではなく、各係数の中身(根拠)にあります。

---

## 3. 冷房負荷・暖房負荷・換気量の算出過程

- **冷房・暖房負荷**:面積 × 用途別原単位(W/m²) × 地域係数 × 温度設定補正 × 1.15 という、単一の掛け算チェーンのみ。中間値(構造体○kW、窓○kW…)は一切生成されず、`coolingLoadKW`という最終値がいきなり出てくる構造です。**途中経過が存在しないため、後から「なぜこの数字になったか」を追跡できません(§15で詳述)。**
- **換気量**:人数×用途別原単位のみ。この式自体はシンプルで追跡可能ですが、原単位表(30/25/20/15 m³/h/人)の出典が明示されていません。

---

## 4. 「安全率1.15」の扱いと採用根拠

- コード上は`SAFETY_FACTOR = 1.15`という定数が、**冷房負荷にも暖房負荷にも無条件に掛かっています**(766・768行目)。
- **採用根拠はコード上どこにも記載されていません。** コメントにも根拠資料への言及がなく、前回監査時点でも「本アプリ独自の仮定」であることを確認済みです。
- **二重掛けの疑い**:負荷計算側で1.15を掛けた上、機種選定側でも標準容量への切り上げにより`surplusPct`という形でさらに余裕が生じます(例:8.0kW×4台=32.0kWは、1.15適用後の28.1kWに対してさらに+13.9%)。つまり実質的な余裕は「原単位法の負荷 × 1.15 × 1.139 ≒ 原単位法の負荷 × 1.31」相当になっており、**どこまでが意図した安全率で、どこからが機種容量の刻みによる結果なのかが数字上区別できません。**
- 前回の設計書(計算式監査・詳細モード仕様書v1.0)で提案した「①計算負荷(係数なし)→②機器の能力補正→③選定機種容量→④選定余裕率(表示のみ)」の4段階構造は、**まだコードに反映されていません。** 現行コードは依然として旧方式(一律1.15)のままです。

---

## 5. 一律の安全率に頼らず、負荷要素を積み上げる設計への変更点

現行コードには以下が**存在しません**(すべて新規追加が必要な変更点です)。

| 不足している負荷要素 | 現状 |
|---|---|
| 構造体貫流負荷 | なし(原単位に内包され分離不可) |
| 窓ガラス貫流負荷 | なし |
| 日射負荷 | なし |
| 人体負荷(顕熱・潜熱の分離) | なし(人数は換気量計算にのみ使用され、発熱計算には未使用) |
| 照明負荷 | なし |
| 機器・厨房発熱 | なし |
| すきま風負荷 | なし |
| 外気負荷(顕熱・潜熱) | なし(換気量[m³/h]は計算されているが、そこから熱負荷[kW]への変換式が存在しない) |

**現在、人数(`occupants`)は換気量計算にのみ使われており、人体発熱として冷房負荷には一切反映されていません。** これは見落としやすい実装上の欠落です。前回の詳細モード仕様書v1.0で設計した`Occupancy`・`Envelope`・`Window`・`Lighting`・`Equipment`のデータモデルへの置き換えが必要ですが、**これも未着手**です。

---

## 6. 外気条件・室内条件・設計条件の扱い

- **室内条件**:冷房・暖房の設定温度(℃)のみ入力可能。**湿度の入力項目はコード上どこにも存在しません。** そのため潜熱負荷(人体潜熱・外気潜熱)は原理的に計算できない状態です。
- **外気条件**:`REGIONS`配列の`coolingFactor`/`heatingFactor`という無次元係数のみで、実際の設計用外気温度(℃)・湿度(%)の数値はコード上どこにも保持されていません。前回仕様書で提示した`Weather`データモデル(設計用外気乾球・湿球温度)は未実装です。
- **設計条件の版管理**:`REGIONS`・`BUILDING_TYPES`・`PACKAGE_SIZES`のいずれにも、出典・版・改訂日を示すフィールドがありません(§11で詳述)。

---

## 7. 外皮・窓・日射・人体・照明・機器・換気・外気負荷等の負荷要素

§5と重複しますが、コード上の実装状況を要素ごとに一覧化します。

| 負荷要素 | コード上の変数 | 実装状況 |
|---|---|---|
| 外皮(構造体) | なし | 未実装 |
| 窓(貫流) | なし | 未実装 |
| 日射 | なし | 未実装 |
| 人体 | `occupants`(換気量計算にのみ使用) | **発熱計算としては未実装** |
| 照明 | なし | 未実装 |
| 機器発熱 | なし | 未実装 |
| 換気(風量) | `ventilationM3h` | **実装済み(m³/hの算出のみ。熱負荷への変換は未実装)** |
| 外気負荷(熱量) | なし | 未実装 |
| すきま風 | なし | 未実装 |

実装済みなのは換気「風量」の算出のみで、換気に伴う「熱負荷」への変換は行われていません。

---

## 8. 機器容量・台数を選定するロジック

775〜783行目のロジックは以下のとおりです。

```
PACKAGE_SIZES の各容量について:
  count = ceil(requiredCapacityKW / 容量)
  installedKW = count × 容量
  surplusPct = (installedKW − requiredCapacityKW) / requiredCapacityKW × 100
recommended = surplusPct が最小のもの
```

**問題点**:
1. `PACKAGE_SIZES`には2.5馬力(7.1kW/63形)が欠落しています(5.6→8.0kWの間に選択肢がなく、実際にはこの間の負荷でも8.0kWに切り上げられてしまいます)。
2. `requiredCapacityKW = max(冷房, 暖房)`という単一の数値に対し、単一の`p.kw`(公称冷房能力相当)で機種を選定していますが、**実在機種の暖房能力は冷房能力と同一ではありません**(前回調査でも、メーカーによって暖房能力が冷房能力と異なる実例を確認しています)。暖房負荷が支配的な地域(例:北海道)では、この選定方法は暖房能力を過小評価する可能性があります。
3. `REAL_EQUIPMENT_80`(実在型式データ)は機器選定・価格比較画面の**表示にのみ**使われており、`recommended`の算出ロジック自体には一切接続されていません。つまり「なぜこの容量が推奨されたか」という計算過程と、「実際にどのメーカーのどの型式が該当するか」という参照データが、コード上完全に分離しています。

---

## 9. 「なぜこの機種・容量・台数を選んだのか」を説明可能にするロジック

**現状、説明可能ではありません。** 理由:

- UI上に表示されている「余裕率+13.9%」等の数値は、`surplusPct`という単一の計算結果であり、その内訳(冷房基準か暖房基準か、どちらの負荷が支配的だったか)を利用者に示す変数がコード上に存在しません。
- `requiredCapacityKW = max(冷房, 暖房)`のあと、どちらが採用されたか(冷房が支配的だったのか暖房が支配的だったのか)を保持する変数がなく、UIにも表示されていません。
- 負荷そのものが単一の原単位法の結果であるため、「窓が大きいから」「日射が強いから」といった要素別の説明も原理的に不可能です。

**この項目こそ、前回の詳細モード仕様書v1.0(`LoadResult`データモデルに各負荷要素を保持する設計)が解決を狙った部分ですが、現行コードには反映されていません。**

---

## 10. JIS・JRAIA等の規格・技術資料とメーカー仕様の混同の有無

- `PACKAGE_SIZES`のコード内コメントに「JIS的な公称能力の呼称」とありますが(41行目)、**JIS規格そのものへの参照(規格番号・条項)はコード上に記載されていません。** 前回調査で確認したJRAIA(日本冷凍空調工業会)の情報(定格試験条件:冷房室内27℃/室外35℃、暖房室内20℃/室外7℃・2℃)も、コード上には一切反映されていません。
- `REAL_EQUIPMENT_80`の各行には`sourceName`・`sourceUrl`フィールドがあり、**個別の型式情報については出典を明示する構造が既にあります(これは良い設計です)。**
- 一方で、`BUILDING_TYPES`の`coolingWm2`等の原単位や`REGIONS`の係数には、こうした出典フィールドが一切ありません。**「型式情報には出典があるが、負荷計算の係数には出典がない」という非対称な状態**になっています。混同というより「片方にしか出典管理の仕組みがない」ことが問題です。

---

## 11. 各数値・計算条件について、根拠資料・版・改訂日を管理できる構造か

**管理できていません。** `BUILDING_TYPES`・`REGIONS`・`PACKAGE_SIZES`・`SAFETY_FACTOR`のいずれも、単なる数値・文字列の配列/定数であり、以下のようなメタデータを持つ構造にはなっていません。

```javascript
// 現状(出典・版の情報を持たない)
{ id: "restaurant", coolingWm2: 244, heatingWm2: 174, ventPerPerson: 30 }

// あるべき構造(例)
{
  id: "restaurant",
  coolingWm2: 244,
  source: { name: "本アプリの概算仮定", revision: null, confirmedDate: null },
  ...
}
```

`REAL_EQUIPMENT_80`のみ`sourceName`/`sourceUrl`を持ちますが、これも「版」「確認日」のフィールドはありません(URLはあっても、いつ確認した情報かが記録されていません)。

---

## 12. 将来、基準・資料が改訂された場合に更新できる構造か

**現状は困難です。** 理由:

- 係数がコード中に直接ハードコードされており(マジックナンバー)、値を変更するにはコード自体を編集する必要があります。設定ファイルやデータベースとして外出しされていません。
- §11のとおり出典・版のメタデータがないため、「この係数はどの資料のどの版に基づくものか」「更新が必要かどうか」を機械的に判定する手段がありません。
- 変更履歴(いつ・誰が・どの根拠で数値を変えたか)を記録する仕組みもありません。

---

## 13. 概算モードと詳細設計モードを分ける場合の精度・前提条件

**現状、モード分離そのものが実装されていません。** 現行コードは「概算モード」1本のみで動いており、詳細モードのUI・データ構造は前回の設計書(v1.0)で設計しただけの段階です。したがって「それぞれの精度・前提条件を明確にできるか」という問いに対する現時点の答えは、**「詳細モードが存在しないため比較のしようがない」**になります。設計書上は概算モード=原単位法(根拠を限定的にしか持たない)、詳細モード=建築設備設計基準に基づく積み上げ法、と役割分担する方針を既に示していますが、コードへの反映はこれからです。

---

## 14. 「100㎡・5階・30人・飲食店」テストケースの入力反映の正しさ

現行コードの初期値(721〜728行目)は次のとおりです。

```javascript
buildingTypeId = "restaurant"
floorArea = 100
floors = 5
occupants = 30
coolingSetTemp = 26
heatingSetTemp = 22
regionId = "kanto"
```

**ここに構造的な問題があります。** `floors`(階数=5)は、759行目で`nFloors`として取得されますが、**負荷計算式(`coolingLoadKW`等)には一切使用されていません。** `floors`が使われるのは`equipmentOptions`内の`perFloor = count / nFloors`(781行目、「1台あたり/階」の表示用)だけです。

つまり現行コードは、`floorArea = 100`を**「建物全体でただ1回だけ」**式に投入しており、「5階建てなので延床500m²」という解釈には**なっていません**。これは前回の詳細モード仕様書v1.0・§1-1で指摘した「100m²が延床なのか1フロアなのか」という設計上の課題が、**現行の実装ではどちらの解釈かを明示しないまま「100m²を1回だけ使う(=事実上、延床100m²として扱う)」という暗黙の選択をしている状態**であることを意味します。「5階」という入力が計算結果に与える影響が実質ゼロである点は、ユーザーの直感(5階建てなら床面積の合計が増えるはず)と一致しない可能性があり、**テストケースの入力が計算に正しく反映されているとは言い切れません。**

その他の入力(`occupants=30`→換気量900m³/h、`coolingSetTemp=26`→温度補正係数1.0等)は、式への反映自体は機械的に正しく行われています。

---

## 15. 計算結果を第三者が追跡・検証できるか

**現状、限定的にしかできません。**

- 良い点:「計算根拠を表示」トグル(コード内`showBasis`)により、原単位・地域係数・温度補正係数・安全率の**係数の値自体**は画面に表示され、電卓で再計算すれば`coolingLoadKW`の値を検証すること自体は可能です。
- 悪い点:
  1. 係数の**出典**が示されないため、「この244W/m²という数字はどこから来たのか」を第三者が検証する手段がありません。
  2. 負荷が単一の掛け算のみで構成されるため、「窓が大きいから高い」「断熱が悪いから高い」といった**要素分解された説明**ができません。
  3. `surplusPct`の分母(`requiredCapacityKW`)が0になるケース(例:面積0、または両用途の原単位が偶然0になる入力)で`Infinity`や`NaN`が生じる可能性があり(775〜781行目、および機器選定画面323〜325行目でも同様の割り算あり)、**この場合に画面がどう表示されるかコード上検証しておらず、異常値が出た際に第三者が気づける保証がありません。**
  4. 機器選定の`recommended`と実在型式(`REAL_EQUIPMENT_80`)が接続されていないため、「なぜこの型式なのか」まで遡って検証することができません(§8・§9で詳述)。

---

## 一覧表(15項目 総括)

| No | 現状 | 問題点 | 修正すべき点 | 根拠 | 今後の推奨設計 |
|---|---|---|---|---|---|
| 1 | 単一の掛け算チェーンのみ | 中間値が存在せず要素分解不可 | 負荷要素ごとの式に分割 | コード757〜796行目 | 詳細モード仕様書v1.0 §5・§6 |
| 2 | 単位換算は正しい | なし | ― | ― | ― |
| 3 | 冷房・暖房負荷・換気量とも算出可能 | 冷房・暖房負荷は途中経過なし | 積み上げ計算への変更 | 同上 | 同上 |
| 4 | 一律1.15を無条件適用 | 根拠不明、二重掛けの疑い | 4段階構造(計算負荷→能力補正→選定容量→余裕率)へ | 計算式監査レポート(前回) | 詳細モード仕様書v1.0 §8 |
| 5 | 要素積み上げ皆無 | 構造体・窓・日射・人体・照明・機器発熱・すきま風・外気が未実装 | Envelope/Window/Occupancy/Lighting/Equipmentモデルの実装 | ― | 詳細モード仕様書v1.0 §10 |
| 6 | 温度設定のみ入力可、湿度・外気設計温湿度は未保持 | 潜熱負荷が原理的に計算不能 | DesignCondition・Weatherモデルの実装 | ― | 同上 §2・§4 |
| 7 | 換気風量のみ実装、他は未実装 | §5と同様 | 同上 | ― | 同上 |
| 8 | 単一requiredCapacityKWで機種選定 | 2.5馬力欠落、暖房能力≠冷房能力を無視、実在型式と非接続 | PACKAGE_SIZES補完、冷暖別能力での選定、REAL_EQUIPMENT連携 | コード41〜48・775〜783行目 | 詳細モード仕様書v1.0 §8 |
| 9 | 説明変数なし | 「なぜこの容量か」を辿れない | LoadResultに要素別内訳を保持しUIに表示 | ― | 詳細モード仕様書v1.0 §10(LoadResult) |
| 10 | 型式情報のみ出典あり、負荷係数は出典なし | 非対称な出典管理 | 全ての数値に出典フィールドを付与 | コード57〜122行目 vs 17〜37行目 | §11参照 |
| 11 | 出典・版・確認日のメタデータなし | 改訂追跡不可能 | 出典・版・確認日を持つデータ構造へ | ― | 本レポート§11の例 |
| 12 | 係数がハードコード、変更履歴なし | 基準改訂時に更新困難 | 設定の外出し・変更履歴管理 | ― | ― |
| 13 | モード分離未実装 | 精度差を提示できる段階にない | 概算/詳細モードの実装 | ― | 詳細モード仕様書v1.0 全体 |
| 14 | floorArea=100が階数によらず1回のみ使用 | 「5階」が計算に無反映、テストケースの意図とズレる可能性 | Building/Floorモデルで面積を明示的に扱う | コード724〜728・759・781行目 | 詳細モード仕様書v1.0 §1-1 |
| 15 | 係数値は検証可能、出典・要素分解・0除算耐性は不可 | 第三者検証性が限定的 | 出典明示・要素分解・エッジケース処理 | コード775〜781・323〜325行目 | 本レポート§9・§11 |

---

## 今後の推奨設計(優先順位案)

これは提案であり、まだ着手していません。

1. **最優先**:§14で指摘した「階数が負荷計算に反映されない」問題は、テストケースの解釈そのものに関わるため、詳細モード実装前に方針(延床合計を使うか、フロアごとに計算するか)を確定すべきです。
2. **次点**:§4の安全率4段階構造は、コードの変更量が比較的小さく、実務上の説明力に直結するため、詳細モード全体の前に先行して着手する価値があります。
3. **本丸**:§5〜§9の要素積み上げ計算(詳細モード仕様書v1.0のデータモデル)。ここが「実務で使えるレベル」への到達に最も影響します。
4. **並行**:§10〜§12の出典・版管理の仕組みは、要素積み上げ計算の実装と同時に組み込むのが効率的です(あとから全数値に出典を付け直すのは手戻りが大きいため)。

コードの変更は、この監査内容についてご確認いただいた後に着手します。UIワイヤーフレーム設計にもまだ進んでいません。
