# 計算エンジン 業界基準適合性レビュー

作成日: 2026-09-02
対象: `hvac-calc-engine.js`(SHARED-LOGICブロック、`hvac-load-calculator.jsx`と同一)
方針: コードは変更しません。今回は監査のみです。

---

## 0. 監査の前提となる重大な発見

国土交通省「建築設備設計基準 令和6年版」(mlit.go.jp/gobuild/content/001390961.pdf、969ページの印刷版のうち無償公開されているのは本文29ページ)の**全文を確認しました。** その結果、以下が判明しました。

**この公開文書には、具体的な計算式・係数表・数値基準が一切含まれていません。** 全編にわたり「〜に基づき算定する」「〜を考慮して選定する」という定性的な指針の列挙であり、原単位(W/m²)、熱貫流率(U値)、人体発熱表、遮蔽係数、換気量係数などの**実際の数値は、有償の別冊「建築設備設計基準の資料」(令和6年3月8日国営設第160号、公共建築協会から書籍として販売、Web上で無償公開されていない)にのみ収録されています。**

これは監査の前提として重要です。本レビューで「一致」と判定できるのは**計算の考え方・構造・対象項目**のみであり、「244W/m²のような個別の数値そのもの」を国土交通省の基準と直接照合することは、無償で公開されている情報の範囲では原理的に不可能です。この制約を偽らず明示します。

参照した一次資料:
- 建築設備設計基準 令和6年版(国営設第159号)全文(mlit.go.jp/gobuild/content/001390961.pdf)
- 建築設備計画基準 令和6年版(国営設第157号)一部(mlit.go.jp/gobuild/content/001390958.pdf)
- 日本冷凍空調工業会(JRAIA)公式サイト(前回セッションで確認済み、機器定格試験条件)
- 公共建築工事標準仕様書(機械設備工事編)令和7年版:今回、無償公開されている本文を発見できませんでした(未確認)

---

## 1〜22. 項目別監査表

| No | 項目 | 現在の実装/計算式/係数 | 根拠資料 | 該当箇所 | 一致/不一致/根拠不明 | 修正要否 |
|---|---|---|---|---|---|---|
| 1 | 冷房負荷計算 | `roughLoadCoolingKW = area×coolingWm2×regionFactor×tempFactor`(原単位法、単一係数の掛け算) | 設計基準 第4編第1章第2節 | 上記(3) | **不一致**。基準は8項目の個別積み上げを要求しており、単一原単位への集約は基準が定める算定構造そのものとは異なる | 要修正(詳細モードで対応予定。現行は「概算モード」である旨の明示が必須) |
| 2 | 暖房負荷計算 | `roughLoadHeatingKW = area×heatingWm2×regionFactor×tempFactor` | 同上 | 上記(4) | **不一致**(1と同じ理由)。ただし「暖房には内部発熱を含めない」という設計思想は、人体負荷を暖房に加算しない現行実装と**一致** | 要修正(積み上げ化)。暖房に人体負荷を加算しない点は現状維持でよい |
| 3 | 構造体負荷 | **未実装**(原単位に内包、分離不可) | 設計基準 冷房①・暖房① | 同上 | 不一致(基準は独立項目として算定を要求) | 要実装(詳細モードの範囲) |
| 4 | ガラス面負荷 | **未実装** | 設計基準 冷房②・暖房② | 同上 | 不一致 | 要実装 |
| 5 | 日射負荷 | **未実装** | 設計基準 冷房③ | 同上 | 不一致 | 要実装(気象データが必要、既知の制約) |
| 6 | 照明負荷 | **未実装** | 設計基準 冷房③(照明負荷) | 同上 | 不一致 | 要実装 |
| 7 | 人体負荷(顕熱・潜熱) | `occupantSensibleKW=occupants×60/1000`、`occupantLatentKW=occupants×50/1000`。**既定では設計用必要負荷に不算入**(includeOccupantLoad=false) | 設計基準は冷房④として要求。ただし60W/50Wという数値自体は**実務解説記事(株式会社パラダイム)の目安値であり、公的資料ではない** | 設計基準第4編第1章第2節(3)④(項目の存在のみ) | **項目の存在は一致、数値の根拠は不明**。さらに「既定で不算入」という現行の運用は、基準が要求する「対象として算定する」という原則とは**厳密には不一致**(算定はするが計算結果に含めない、という状態) | 数値要調査(SHASE便覧等)。運用方針は次回要検討 |
| 8 | 外気負荷 | **未実装**(換気量[m³/h]は算出しているが、熱負荷[kW]への変換式が存在しない) | 設計基準 冷房⑦・暖房④ | 同上 | 不一致 | 要実装 |
| 9 | すきま風負荷 | **未実装** | 設計基準 冷房⑥・暖房③、算定式は設計基準に明記(前回セッションで確認済み) | 同上 | 不一致(基準は算定式まで明記しているが未実装) | 要実装。基準の式がそのまま使える数少ない項目 |
| 10 | その他室内負荷 | **未実装**(機器発熱・厨房発熱を含む) | 設計基準 冷房⑤ | 同上 | 不一致 | 要実装 |
| 11 | 換気・外気条件 | `ventilationM3h = occupants × ventPerPerson`(用途別固定原単位) | 設計基準は「許容されるCO2濃度、温湿度、化学物質濃度等に基づき算定する」と規定 | 第4編第4章第3節 | **不一致**。現行は人数×固定原単位という簡略式で、CO2濃度等に基づく算定ではない。原単位の値自体も出典未確認 | 出典調査要。当面は「簡易換算式」である旨の明示が必須 |
| 12 | 地域・外気条件 | `REGIONS`の`coolingFactor`/`heatingFactor`という無次元係数(関東=1.0基準) | 設計基準は「指定された温湿度条件や屋外気象条件に基づき」算定するよう規定 | 第4編第1章第2節(1) | **不一致**。基準は実際の外気設計温湿度データ(気象データ)の使用を求めており、単一の無次元係数への単純化は基準の方法と異なる | 要修正。EA気象データ等の導入が必要(既知の制約、前回セッションで確認済み) |
| 13 | 方位 | **未実装** | 設計基準は日射負荷の算定を要求しており方位情報が前提 | 同上 | 不一致 | 要実装 |
| 14 | 階数・対象階 | 階数は負荷計算に不使用(表示上の配分のみ)。面積は「延床面積(合計)」として1回のみ使用 | 設計基準に明示的な規定なし(階数の扱いについて直接言及する条文は確認できず) | ― | 該当条文なし(是非を判断する基準がない) | 現行の運用方針(前回セッションで確定)を維持。詳細モードでフロア別対応を検討 |
| 15 | 用途別条件 | `BUILDING_TYPES`(8用途、原単位固定) | 設計基準「室内温湿度条件は、対象となる各室の用途に応じて選定する」 | 第4編第1章第2節(2) | **考え方は一致**(用途に応じた条件設定という方針)。ただし**個別の数値(244W/m²等)の出典は不明** | 数値要調査 |
| 16 | 温湿度条件 | 室内設定温度のみ(冷房26℃・暖房22℃)。**湿度の入力項目自体が存在しない** | 設計基準「指定された温湿度条件」に基づき算定 | 同上 | **不一致**。基準は温湿度両方を要求しているが、現行は温度のみで湿度が欠落 | 要修正(湿度入力の追加は詳細モードの範囲) |
| 17 | 必要能力の算定 | `requiredCapacityKW = max(designLoadCooling, designLoadHeating)` | 設計基準「熱源機器及び空気調和機の能力を決めるために空調熱負荷を算定する」 | 第4編第1章第1節(1) | **考え方は一致**(冷暖房負荷から能力を決定するという構造)。ただし負荷そのものの精度は1〜16の不一致を引き継ぐ | 負荷精度の改善が前提 |
| 18 | 機器能力選定 | `EQUIPMENT_DB`(実在型式の冷房公称能力のみ保持、暖房能力は全件`null`) | JRAIA公式サイト(機器定格試験条件:冷房室内27℃DB/室外35℃、暖房標準室内20℃/室外7℃、暖房低温室外2℃) | 前回セッションで確認済み(JRAIA公式サイト) | **能力補正(JIS定格条件と設計条件のズレの補正)は未実装**。実在機種の冷房公称能力を単純に負荷と比較しているのみで、能力補正曲線を使っていない | 要実装(前回設計書§8「②機器の能力補正」で設計済みだが未実装) |
| 19 | 台数選定 | `count = ceil(requiredCapacityKW / p.kw)`(数学的な切り上げ除算のみ) | 設計基準「熱源機器の台数は、施設の規模、用途、熱負荷傾向、使用時間帯、機器効率、保守点検等を考慮して算定する」 | 第4編第1章第3節(1) | **不一致**。基準は規模・用途・負荷傾向・使用時間帯・機器効率・保守点検等、**多数の定性的要因を考慮した選定**を求めているが、現行は容量の数学的な最小構成のみ。初期概算としては妥当だが「正式選定」ではない | 「容量クラス仮選定」である旨を強調する運用は既に導入済み。定性要因を扱うUIは対象外(今回スコープ外) |
| 20 | 余裕率・安全率 | ①概算負荷(係数なし)→②計画上の余裕(初期値0%・任意入力)→③機器選定→④選定余裕率(表示のみ)、の4段階に分離済み | 設計基準は「熱源機器容量は、建物時刻別負荷集計の最大値に基づき算定する」と規定するのみで、**特定の安全率・余裕率の数値を指定していない** | 第4編第1章第3節(4) | **構造としては矛盾なし**(基準は特定係数を強制していないため、現行の「係数を機械的に掛けない」設計は基準と対立しない)。ただし「建物時刻別負荷集計の最大値」という基準の要求(時刻別計算)自体は未実装 | 時刻別計算は詳細モードの範囲(前回設計書で言及済み、未実装) |
| 21 | 現在使用している係数・固定値(一覧) | `coolingWm2`/`heatingWm2`(8用途×2)、`ventPerPerson`(8用途)、`coolingFactor`/`heatingFactor`(8地域×2)、`0.03`(温度設定補正/℃)、`60W`/`50W`(人体発熱) | いずれも本アプリ独自の仮定、または実務解説記事(非公的)。設計基準の公開範囲内に該当数値は存在しない | ― | **すべて根拠不明(公的資料での確認不可)** | すべて「未確定・要検証」の明示が必要 |
| 22 | 244W/m²(飲食店・冷房原単位)の根拠 | `BUILDING_TYPES`内の固定値 | **本アプリ開発初期(本セッション最初のプロトタイプ作成時)に、外部資料を参照せず設定した値。国土交通省・JIS・JRAIA・日本建築学会のいずれの資料にも同数値の記載を確認できませんでした** | ― | **未確定**。「A:含む/B:含まない」の判定も、そもそも数値自体の出典がないため判定不能 | **正式仕様として確定できません。詳細モードでの積み上げ計算への置き換えが必要** |

---

## ① 現在の計算エンジンは業界基準に対して何%程度まで確認できているか

**数値レベルでは0%、構造・考え方のレベルでは40〜50%程度**という2段階の評価になります。

- 「原単位244W/m²のような個別の数値」が国土交通省基準と一致するかどうかは、そもそも当該基準の無償公開範囲に数値そのものが存在しないため、**確認可能な範囲では0%**です(有償の「資料」編を確認できれば変わる可能性があります)。
- 「負荷を何によって構成するか」「安全率をどう扱うか」「機器容量をどう決めるか」といった**計算の構造・考え方**については、22項目中、明確に一致または整合していると言えるものが4〜5項目(14・15・17・20の一部)、明確に不一致が13項目、判定不能(条文なし)が1項目という内訳で、**構造面ではおおよそ4割程度が基準の考え方に沿っている**という評価です。

## ② 一致している項目

- **項目15(用途別条件)**:「用途に応じて条件を選定する」という考え方自体は一致
- **項目17(必要能力の算定)**:「熱負荷から機器能力を決定する」という構造は一致
- **項目20(余裕率・安全率)**:基準が特定係数を強制していないため、現行の「機械的な係数を掛けない」設計は基準と対立しない
- **項目2の一部(暖房に内部発熱を含めない)**:設計思想が一致
- **項目9(すきま風負荷)**:基準が算定式まで明記している点で、対応方針(実装すればそのまま使える)が明確

## ③ 不一致の項目

項目1・2(原単位法vs積み上げ法)・3〜6・8・10(未実装の個別負荷)・11(換気量算定根拠)・12(地域係数vs気象データ)・13(方位未実装)・16(湿度欠落)・18(能力補正未実装)・19(台数選定の定性要因未考慮)。**22項目中13項目が明確な不一致です。**

## ④ 根拠が確認できない項目

項目7(人体発熱60W/50W)・11(換気量原単位)・15(原単位の個別数値)・21(全係数)・22(244W/m²)。**これらはいずれも「本アプリの仮定」または「非公的な実務解説記事」が出典であり、国土交通省・JIS・JRAIA・日本建築学会のいずれの資料でも確認できていません。**

## ⑤ 244W/m²の判定

**未確定です。** 出典を確認できないため、A(人体発熱を含む)/B(含まない)のいずれとも判定できません。この値を含む原単位法は、当面「本アプリの概算モデル」として明示を継続し、正式仕様には確定しません。詳細モードでの積み上げ計算(構造体・ガラス・日射・照明・機器発熱を個別算定する方式)に置き換わった時点で、この数値自体が不要になります。

## ⑥ 修正が必要なコード

今回は監査のみでコードは変更していません。今後修正が必要になる箇所(優先順位順):
1. `hvac-calc-engine.js`の`computeLoad()`:原単位法から積み上げ計算への置き換え(項目1〜6・8・10・13・16)
2. `EQUIPMENT_DB`:能力補正曲線データの追加(項目18)
3. `selectEquipment()`:定性的選定要因を扱うための拡張、または「容量クラス仮選定」であることの一層の強調(項目19)
4. `REGIONS`:気象データベースへの置き換え(項目12)

## ⑦ 修正前後で計算結果がどう変わるか

現時点では**具体的な変化量を提示できません。** 積み上げ計算に必要な入力(窓面積・断熱仕様・方位・湿度等)がまだ存在しないため、「修正後の数値」を試算する前提条件が揃っていません。前回の詳細モード仕様書v1.0(具体例A・B)で示したとおり、建物仕様次第で現行値より増加する場合・減少する場合の両方があり得ます。

## ⑧ 修正後に必要なテスト

1. 積み上げ計算の各項目(構造体・ガラス・日射等)の単体テスト(項目ごとの単位・境界値)
2. 原単位法(概算モード)と積み上げ法(詳細モード)を同一建物条件で計算した際の乖離率の記録(異常な乖離がないかの確認)
3. 既存31件のテストのうち、原単位法に依存するテスト(面積比例性等)は詳細モード導入後も概算モード側のテストとして維持し、詳細モード用の新規テストを別途追加
4. 能力補正曲線を追加した場合の、補正前後での機種選定結果の差分テスト

## ⑨ 現時点で「実務利用可能」と言える範囲

**「実施設計・正式な機器選定・見積確定」の根拠としては実務利用不可です。** 数値根拠が確認できていないため。一方、**「初期段階のスケール感(おおよそ何kWクラス、何台程度か)をつかむための概算ツール」としては、その旨を明示すれば利用可能**と考えます。現行アプリの随所にある「概算」「要確認」「仮データ」表示は、この限界を反映したものとして機能しています。

## ⑩ 次にやるべき工程

新機能追加ではなく、次の2択のいずれかを推奨します。
1. **有償の「建築設備設計基準の資料」(国営設第160号)を入手し、原単位・U値・人体発熱表等の正式な数値を確認する**(最も確実だが外部購入が必要)
2. **SHASE(空気調和・衛生工学会)の便覧等、入手可能な専門資料で個別係数を1つずつ確認しながら、詳細モードの積み上げ計算を項目ごとに実装していく**(前回の詳細モード仕様書v1.0の実装着手)

いずれにせよ、**244W/m²のような概算モードの数値を「正式仕様」として扱うのはこのレビュー結果に基づき見送り**、「本アプリの概算仮定」であることを明示し続けることを引き続き推奨します。
