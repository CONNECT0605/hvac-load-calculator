# 詳細モード計算仕様書 v1.0

作成日: 2026-09-02
ステータス: **設計書のみ。コードは未変更。**
前提: React/JavaScriptでの実装を想定したデータ構造・計算接続の設計。

---

## 0. 表記ルール

- 「根拠」欄に出典を明記できないものは、数式であっても **「本アプリの仮定(要確認)」** と明記します。「業界標準」「一般的」とは書きません。
- 主な一次資料: **建築設備設計基準(国土交通省大臣官房官庁営繕部設備・環境課監修、令和6年版)**。以下「設計基準」と略します。
- 機器定格条件の出典: **一般社団法人 日本冷凍空調工業会(JRAIA)公式サイト「業務用エアコンの能力表示について」**(cite index="62-1">。以下「JRAIA」と略します。</cite>
- 気象データの出典: **拡張アメダス気象データ(EA気象データ)**、(株)気象データシステム(MetDS)発行、空気調和・衛生工学会(SHASE)の熱負荷計算ツール「HASPEE」が採用(cite index="72-1,74-1">。以下「EA気象データ」と略します。このデータは有償・ライセンス契約が必要であり、本アプリに無条件で内蔵することはできません。</cite>
- SHASE「空気調和・衛生工学便覧」原文は今回のセッションでも未参照です。詳細モードの実装着手前に別途入手・確認が必要な項目として扱います。

---

## 1. 建物基本条件

### 1-1. 「100m²・5階」問題の解消

現行のテスト条件は「延床面積100m²・階数5階」という入力が並んでおり、**100m²が建物全体の延床面積なのか、1フロアあたりの面積なのかが構造上あいまい**でした。これを解消するため、`Building`(建物全体)と`Floor`(階別)を分離し、面積は必ず`Floor`単位で持たせ、`Building.totalFloorArea`は`Floor`の合計から自動算出する構造にします。

```
Building.totalFloorArea = Σ Floor.floorArea (全Floorの合計、自動計算・手入力不可)
```

これにより「100m²は1フロアの面積」「5フロアあるので延床500m²」という扱いも、「延床100m²を5フロアで按分(1フロアあたり20m²)」という扱いも、どちらも明示的に選べます。今回のテスト条件(飲食店・100m²・在室30人)は、**単一フロアの飲食店(Floor 1件、floorArea=100m²)** として扱うのが最も自然な解釈です。この前提を仕様として明記します。

### 1-2. 入力項目表

| 項目 | 単位 | 入力方法 | 初期値 | 計算に使用する場所 | データソース | 根拠 | 変更可否 | 未入力時の扱い |
|---|---|---|---|---|---|---|---|---|
| 用途(Building.usage) | 区分選択 | セレクト | 飲食店 | 内部発熱原単位・換気量原単位の参照キー | ユーザー入力 | ― | 可 | 選択必須(既定値あり) |
| 所在地(Building.location) | 都市/地域選択 | セレクト | 関東(東京) | Weather参照キー | ユーザー入力 | ― | 可 | 選択必須 |
| 階数(Building.floorCount) | 階 | 数値 | 1 | Floor配列の件数と整合チェック | ユーザー入力 | ― | 可 | 1として扱う |
| 対象階(Floor.floorIndex) | 階番号 | 数値 | 1 | 表示・整理用。外気接地面(1階か否か)の判定に使用 | ユーザー入力 | ― | 可 | 1階として扱う |
| 各階床面積(Floor.floorArea) | m² | 数値 | 100 | 内部発熱・人体負荷・すきま風等の面積按分計算の基礎 | ユーザー入力 | ― | 可 | 空調対象室の合計から逆算(要確認表示) |
| 延床面積(Building.totalFloorArea) | m² | **自動計算(入力不可)** | Σ Floor.floorArea | 表示・機器容量按分の参考値 | Floor集計 | ― | 不可(Floorの合計) | ― |
| 階高(Floor.floorHeight) | m | 数値 | 3.2 | 室容積計算(すきま風負荷、外気負荷の絶対湿度換算等) | ユーザー入力 | ― | 可 | 3.0m(仮定・要確認) |
| 天井高(Room.ceilingHeight) | m | 数値 | 2.6 | 室容積計算 | ユーザー入力 | ― | 可 | 階高より天井懐(0.6m)を差し引いた値(仮定) |
| 空調対象室(Room[]) | 室のリスト | 室ごとに追加 | Floorにつき1室(全体を1室として計算) | 負荷計算の最小単位。全ての室負荷はRoom単位で積み上げ、Floor・Building単位に集計 | ユーザー入力 | ― | 可(室を分割可能) | Floor全体を1室として扱う |

---

## 2. 室条件

| 項目 | 単位 | 入力方法 | 初期値 | 計算に使用する場所 | 計算式との接続 | データソース/根拠 | 変更可否 | 未入力時の扱い |
|---|---|---|---|---|---|---|---|---|
| 室内設計温度(冷房) | ℃DB | 数値 | 26 | 構造体・ガラス面貫流負荷のΔT、外気負荷のΔT | `ΔT = T_out − T_room` | ユーザー入力 | 可 | 26℃(本アプリ仮定・要確認) |
| 室内設計湿度(冷房) | %RHまたは絶対湿度 | 数値 | 50%RH | 外気負荷の潜熱分計算 | `Q_oa_latent = V_oa×ρ×(X_out−X_in)×r` | ユーザー入力 | 可 | 50%RH(本アプリ仮定・要確認) |
| 室内設計温度(暖房) | ℃DB | 数値 | 22 | 同上(暖房側) | 同上 | ユーザー入力 | 可 | 22℃(本アプリ仮定・要確認) |
| 室内設計湿度(暖房) | %RH | 数値 | 40% | 同上 | 同上 | ユーザー入力 | 可 | 40%RH(本アプリ仮定・要確認) |
| 人員(Occupancy.count) | 人 | 数値 | 30 | 人体顕熱・人体潜熱負荷 | `Q=人数×W/人` | ユーザー入力 | 可 | 人員密度×床面積から逆算 |
| 人員密度(Occupancy.density) | 人/m² | 数値(参考表示) | floorArea/人数から自動算出 | 人員未入力時の代替 | 上記 | ユーザー入力 or 自動算出 | 可 | ― |
| 人体発熱(顕熱・潜熱) | W/人 | 作業強度を選択→係数を参照 | 飲食店:接客・調理作業を想定した係数(要確認) | 人体顕熱・潜熱負荷 | 同上 | 実務解説記事(パラダイム社)の目安値(cite index="53-1">事務作業で顕熱約60W/人・潜熱約50W/人という値が示されているが、公的基準ではない</cite>。作業強度別の正式な表はSHASE便覧等で別途確認が必要 | 可(数値上書き可) | 事務作業の目安値を暫定使用し「要確認」表示 |
| 照明負荷 | W/m² | 数値 | 用途別デフォルト(飲食店で仮15W/m²) | 照明負荷 | `Q=床面積×W/m²` | 実務解説記事の目安値(cite index="53-1">。公的基準ではない</cite> | 可 | デフォルト値使用・要確認表示 |
| 機器・厨房等内部発熱 | W/m²または機器積み上げ | 数値または機器リスト入力 | 未入力(0) | その他の室内負荷(設計基準の「⑤その他の室内負荷」に整理)(cite index="49-1">) | `Q=Σ機器発熱` または `Q=床面積×W/m²` | ユーザー入力(機器積み上げ推奨)。積み上げがない場合の目安値は根拠が確認できていないため未設定 | 可 | 0として計算し「厨房発熱未入力」の警告を表示 |
| 使用時間帯 | 時刻範囲 | 開始・終了時刻 | 11:00–22:00(飲食店の仮定) | 最大負荷評価の時間帯絞り込み | §7参照 | ユーザー入力 | 可 | 8:00–20:00(本アプリ仮定・要確認) |
| 空調運転時間 | 時刻範囲 | 開始・終了時刻 | 使用時間帯と同期 | 間欠運転補正(蓄熱負荷)の要否判定 | §2-B⑩(未実装・代替方法は設計仕様書10章参照) | ユーザー入力 | 可 | 使用時間帯と同一とみなす |
| 外気量 | m³/h | 自動計算(現行ロジック流用) | 人数×原単位 | 外気負荷(顕熱・潜熱) | 現行の換気量計算式を流用 | ユーザー入力+現行アプリの原単位表 | 可 | 現行どおり |
| 換気方式 | 区分選択 | セレクト(第1種/第2種/第3種、全熱交換の有無) | 第3種(仮定) | 外気負荷計算時に全熱交換器の熱回収効果を反映するかどうかの分岐 | 全熱交換ありの場合、`Q_oa`に交換効率を乗じて低減 | ユーザー入力 | 可 | 全熱交換なしとして計算(安全側) |
| すきま風・開口部条件 | サッシ気密性区分(良好/中程度/不良)、外部に面する扉の有無・風上/風下 | セレクト | 中程度・扉なし(仮定) | すきま風負荷 | 設計基準の式(cite index="47-1">をそのまま使用</cite> | 設計基準 | 可 | 「中程度」を仮定し要確認表示 |

---

## 3. 建築外皮

| 項目 | 単位 | 入力方法 | 初期値 | 計算に使用する場所 | データソース/根拠 | 変更可否 | 未入力時の扱い |
|---|---|---|---|---|---|---|---|
| 外壁面積(Envelope.wallArea) | m² | 数値(方位別入力可) | 未入力 | 構造体貫流負荷 | ユーザー入力(仕様書等) | 可 | 床面積・階高から簡易推定(要確認)、推定できない場合は概算モードにフォールバック |
| 外壁U値 | W/(m²·K) | 数値または断熱仕様区分から選択 | 未入力 | 構造体貫流負荷 | 仕様書の値。断熱区分別の代表値は本アプリ内蔵の暫定テーブル(要確認、出典未確定) | 可 | 概算モードにフォールバック |
| 屋根面積・U値 | m²、W/(m²·K) | 同上(最上階のみ入力対象) | 未入力 | 構造体貫流負荷(屋根) | 同上 | 可 | 中間階では0扱い、最上階は概算モードにフォールバック |
| 床面積・U値 | m²、W/(m²·K) | 同上(最下階・外部に面する床のみ) | 未入力 | 構造体貫流負荷(床) | 同上 | 可 | 中間階では0扱い |
| 窓面積(Window.area) | m²、方位別 | 数値 | 未入力 | 窓ガラス貫流負荷、日射負荷 | ユーザー入力 | 可 | 概算モードにフォールバック |
| ガラス種類(Window.glassType) | 区分選択(単板/複層/Low-E等) | セレクト | 未入力 | U値・遮蔽係数の参照キー | メーカーカタログ値(本アプリ内蔵テーブルは暫定・要確認) | 可 | 単板ガラス相当(安全側の仮定)として概算 |
| 窓U値 | W/(m²·K) | ガラス種類から自動参照、上書き可 | ガラス種類依存 | 窓ガラス貫流負荷 | 同上 | 可 | 同上 |
| 日射遮蔽係数SC(またはη) | 無次元 | ガラス種類+ブラインド有無から参照 | 未入力 | 日射負荷 | 同上 | 可 | ブラインドなしとして計算(安全側) |
| 方位(Window.orientation) | 8方位選択 | セレクト | 未入力 | 日射量(EA気象データの方位別日射量)の参照キー | EA気象データ(方位別鉛直面日射量) | 可 | 方位不明の場合、南面相当で概算(要確認表示) |
| 隣接室・外気との境界条件 | 区分選択(外気に面する/隣室に面する/地中に面する) | セレクト | 外気に面する(仮定) | 構造体貫流負荷の対象/非対象の判定、ΔTの設定(隣室の場合は室間温度差を別途仮定) | ユーザー入力 | 可 | 外気に面するとして安全側に計算 |

---

## 4. 外気条件(設計用)

「関東=1.0」のような地域係数をやめ、以下の構造にします。

### 4-1. データソースの設計

| データ | 望ましいデータソース | 本アプリでの扱い(現状) |
|---|---|---|
| 設計用外気乾球温度・湿球温度(夏期・冬期) | EA気象データ(拡張アメダス気象データ、MetDS社、有償、SHASE HASPEEが採用)(cite index="72-1,74-1">) | **未取得。ライセンス契約が必要なため、本アプリには組み込めていません。** |
| 方位別鉛直面日射量 | 同上(EA気象データにはPerezの方法による8方位鉛直面日射量が含まれる)(cite index="69-1">) | 同上、未取得 |
| 標準的な設計日(冷房用代表日) | HASPEEでは日射基準タイプにより8/1または9/15等を採用(cite index="69-1">) | 本アプリでは未実装。設計日を固定するか、EA気象データの時刻別値から自動選定するかは今後の実装判断が必要 |

### 4-2. 現段階での代替案(本アプリ内蔵の暫定テーブル)

EA気象データを本アプリに組み込むまでの間、`Weather`データモデルに主要都市の代表的な設計用外気条件を手入力の初期値として持たせる案です。**この暫定テーブルの数値自体は今回のセッションで出典を確認できていないため、「参考値・要確認」のタグを付け、EA気象データ取得後に置き換える前提とします。** 現行の「地域係数(関東=1.0)」よりは実体を持ちますが、正式な設計用気象データではありません。

```
Weather {
  cityId: string
  designDBt_summer: number   // 夏期設計用外気乾球温度(℃) — 要確認・暫定値
  designWBt_summer: number   // 夏期設計用外気湿球温度(℃) — 要確認・暫定値
  designDBt_winter: number   // 冬期設計用外気乾球温度(℃) — 要確認・暫定値
  designRH_winter: number    // 冬期設計用外気相対湿度(%) — 要確認・暫定値
  dataSource: "EA気象データ(未取得)" | "本アプリ暫定値(要確認)"
}
```

---

## 5. 冷房負荷(個別計算構造)

設計基準が定める冷房負荷8項目(cite index="49-1">に対応させます。</cite>

| No | 負荷項目 | 計算式(案) | 入力元データモデル | 根拠 |
|---|---|---|---|---|
| ① | 構造体負荷(顕熱) | `Q₁ = U_wall×A_wall×ΔT + U_roof×A_roof×ΔT + U_floor×A_floor×ΔT` | Envelope, Weather, DesignCondition | 設計基準(項目定義)。U値・ΔTの扱い(実効温度差を使うか単純ΔTか)は本アプリの選択式とし、選択自体を「本アプリの実装方針」と明記 |
| ② | ガラス面負荷(顕熱) | `Q₂ = U_glass×A_window×ΔT` | Window, Weather | 設計基準(項目定義) |
| ③ | 日射負荷(顕熱) | `Q₃ = A_window×SC×I_solar(方位,時刻)` | Window, Weather(EA気象データ) | 設計基準(項目定義)。日射量データは4章のとおりEA気象データが必要 |
| ④ | 照明負荷(顕熱) | `Q₄ = A_room×W_light/m²` | Lighting, Room | 設計基準(項目定義)。原単位は§2の実務解説記事目安値(公的基準ではない) |
| ⑤ | 人体負荷(顕熱) | `Q₅sensible = 人数×顕熱W/人` | Occupancy | 設計基準(項目定義)。原単位は§2参照 |
| ⑤' | 人体負荷(潜熱) | `Q₅latent = 人数×潜熱W/人` | Occupancy | 同上 |
| ⑥ | その他の室内負荷(顕熱・潜熱) | `Q₆ = Σ機器発熱 + 厨房発熱` | Equipment | 設計基準(項目定義)。厨房発熱は§2-B⑨のとおり機器積み上げが望ましい |
| ⑦ | すきま風負荷(顕熱・潜熱) | 設計基準の式(cite index="47-1">をそのまま使用</cite> | Room, Window, Weather | 設計基準 |
| ⑧ | 外気負荷(顕熱・潜熱) | `Q₈sensible=V_oa×ρ×Cp×ΔT`、`Q₈latent=V_oa×ρ×(X_out−X_in)×r` (全熱交換使用時は交換効率を乗じて低減) | Ventilation, Weather, DesignCondition | 設計基準(項目定義)。数式自体は湿り空気線図の基本式 |
| ⑨ | ダクト・配管表面・送風機ポンプ運転・間欠空調蓄熱による負荷 | **本アプリでは未実装**。設計基準は項目のみ定義し算定式は示していない(cite index="49-1">) | ― | 設計基準(項目定義のみ)。代替は既存仕様書の10章参照(定常計算では非考慮とし、その旨明示) |

**冷房負荷合計**: `Q_cooling = Q₁+Q₂+Q₃+Q₄+Q₅sensible+Q₅latent+Q₆+Q₇+Q₈sensible+Q₈latent`(⑨は本アプリでは0として扱い、その旨を画面に明示)

---

## 6. 暖房負荷(個別計算構造)

設計基準が定める暖房負荷5項目(cite index="49-1">に対応させます。日射・照明・人体発熱・その他室内発熱は原則含めません(安全側の考え方)。</cite>

| No | 負荷項目 | 計算式(案) | 根拠 |
|---|---|---|---|
| ① | 構造体負荷(顕熱) | `Q₁ = U_wall×A_wall×ΔT + …`(冷房と同様、ΔTは暖房設計条件) | 設計基準 |
| ② | ガラス面負荷(顕熱) | `Q₂ = U_glass×A_window×ΔT` | 設計基準 |
| ③ | すきま風負荷(顕熱・潜熱) | 設計基準の式(冬期換気回数を使用) | 設計基準(cite index="47-1">) |
| ④ | 外気負荷(顕熱・潜熱) | 冷房と同式(暖房設計条件を使用) | 設計基準 |
| ⑤ | ダクト・配管等負荷、間欠空調蓄熱 | 未実装(冷房同様) | 設計基準(項目定義のみ) |

**暖房負荷合計**: `Q_heating = Q₁+Q₂+Q₃sensible+Q₃latent+Q₄sensible+Q₄latent`(⑤は0として扱い明示)

---

## 7. 最大負荷の決定方法(重要項目)

設計基準は「熱源機器容量は、建物時刻別負荷集計の最大値に基づき算定する」と定めています(cite index="49-1">。これに基づき、以下の方針とします。</cite>

| 論点 | 方針(案) |
|---|---|
| 評価する時間帯 | Room.使用時間帯(§2)の範囲内で、1時間刻みの時刻別負荷を計算し、その最大値を採用する。単純な「ある一時点の値」ではなく、時刻別計算の最大値とする |
| 日射条件の扱い | 方位ごとに日射量が時刻で変わるため、複数方位に窓がある室では合成負荷が時刻によって入れ替わりうる。EA気象データの方位別時刻別日射量を使い、室全体の時刻別合計負荷から最大値を選ぶ(単純に「南面のピーク時刻」を代表させない) |
| 外気条件の扱い | 冷房は夏期設計日、暖房は冬期設計日を想定し、EA気象データの当該設計日の時刻別値を使用する。設計日の選び方(観測データのどの日を「設計日」とするか)はHASPEEの考え方(冷房設計用複数タイプ、暖房設計用複数タイプ)を踏襲するかどうか、実装時に方針決定が必要(cite index="68-1">HASPEEでは冷房設計用3タイプ・暖房設計用2タイプが比較検討されている</cite> |
| 各負荷のピーク時刻 | 項目ごとに個別のピーク時刻を採用するのではなく、**室全体を合計した時刻別負荷のピークを採用する**(項目別ピークの単純合算は過大評価になるため) |
| 単純合算か時刻別計算か | **単純合算(現行の面積原単位法のような「各項目の最大値を無条件に足し合わせる」方式)は採用しない。** 時刻別に室の負荷を積み上げ、その時系列の最大値を「設計最大負荷」とする |
| EA気象データが未取得の場合の代替 | 詳細モードの時刻別計算はEA気象データが前提となるため、データ未取得の間は「代表的な単一時刻(例:夏期14時、冬期早朝)の静的な値」で近似する簡易版を暫定的に用意し、「時刻別計算ではなく代表時刻による近似(要確認)」であることを明示する |

---

## 8. 安全率の再構成

ご指示のとおり、「計算された設計最大負荷 → 機器の実能力・能力補正 → 選定機器容量 → 選定余裕率」の4段階に再構成します。

| 段階 | 内容 | 何のための余裕か | 適用する係数 | 根拠 |
|---|---|---|---|---|
| ① 計算された設計最大負荷 | §7で算出した時刻別最大負荷 | ― | 係数なし(そのままの計算値) | 設計基準 |
| ② 機器の実能力・能力補正 | エアコンのカタログ定格能力は、JIS準拠の標準条件(冷房:室内27℃DB/湿球19℃、室外35℃。暖房標準:室内20℃、室外7℃/湿球6℃。暖房低温:室外2℃/湿球1℃)で測定されている(cite index="62-1">。設計条件がこの標準条件と異なる場合、メーカー公表の能力補正曲線・補正表を用いて実際の発揮能力に換算する</cite> | 定格条件と実際の設計条件のズレを補正するため | メーカー別の能力補正表(本アプリ未実装、要収集) | JRAIA、各メーカーカタログ |
| ③ 選定機器容量 | 標準機種容量(50形=5.6kW、80形=8.0kW…)の中から、②で補正した実発揮能力が①の負荷を上回るものを選ぶ | ― | 機種容量の離散性そのもの(意図的な係数ではない) | メーカーカタログ |
| ④ 選定余裕率 | ③の合計容量が①の負荷に対してどれだけ上回っているかを表示する(現行の「余裕率」表示に相当) | 機種容量が離散的であることによる結果を可視化するため。意図的な安全率の上乗せではない | 表示のみ(計算に係数を掛けない) | ― |

**二重掛けの防止**: ①には係数を掛けません。②はメーカー補正であり設計側が決める数値ではありません。④は結果表示であり入力ではありません。「安全率」という名前の係数は、この4段階のどこにも登場しません。もし発注者・設計者の判断で意図的な余裕(例:将来の用途変更を見込む等)を追加したい場合は、③の前段に「計画上の余裕(理由・数値をユーザーが明示的に入力する項目)」として独立させ、自動では掛けない設計とします。

---

## 9. 入力値の総括表

§1〜§4の表がこの総括表を兼ねています(項目数が非常に多いため、カテゴリ別に既に整理済みです)。総括すると、必須入力(現行アプリ相当)と詳細モード追加入力は以下のとおり分かれます。

| 区分 | 項目数(目安) | 未入力時のフォールバック |
|---|---|---|
| 建物基本条件(§1) | 8項目 | Floor未入力時は概算モード相当に自動フォールバック |
| 室条件(§2) | 12項目 | 個別に仮定値へフォールバック(すべて「要確認」表示) |
| 建築外皮(§3) | 9項目 | 外皮情報が不足する場合、その室は詳細モードで計算不可となり、概算モードの結果を表示した上で「外皮情報未入力のため概算モードの値を表示中」と明示 |
| 外気条件(§4) | 4項目 | EA気象データ未取得のため、暫定値使用を明示 |

---

## 10. データ構造(React/JavaScript想定)

```javascript
// 建物全体
interface Building {
  id: string;
  usage: BuildingUsage;         // "restaurant" | "office" | ...
  locationId: string;           // Weatherを参照
  floorCount: number;
  floors: Floor[];
  get totalFloorArea(): number; // Σ floors.floorArea (自動計算)
}

// 階
interface Floor {
  id: string;
  floorIndex: number;
  floorArea: number;            // m²
  floorHeight: number;          // m
  rooms: Room[];
}

// 室(負荷計算の最小単位)
interface Room {
  id: string;
  name: string;
  floorArea: number;            // m²
  ceilingHeight: number;        // m
  designCondition: DesignCondition;
  envelope: Envelope;
  occupancy: Occupancy;
  lighting: Lighting;
  equipment: Equipment;
  ventilation: Ventilation;
  infiltration: InfiltrationCondition;
}

// 室内設計条件
interface DesignCondition {
  coolingDB: number;   // ℃
  coolingRH: number;   // %
  heatingDB: number;   // ℃
  heatingRH: number;   // %
  operatingHours: { start: string; end: string };
}

// 外皮
interface Envelope {
  walls: WallSurface[];
  roof?: RoofSurface;
  floor?: FloorSurface;
  windows: Window[];
  adjacency: "outdoor" | "adjacent_room" | "ground";
}

interface WallSurface {
  orientation: Orientation;     // "N"|"NE"|"E"|...
  area: number;                 // m²
  uValue: number;                // W/(m²K)
}

interface Window {
  orientation: Orientation;
  area: number;                  // m²
  glassType: GlassType;          // "single" | "double" | "lowE" | ...
  uValue: number;                 // W/(m²K)
  shadingCoefficient: number;    // SC値
  blind: boolean;
}

// 人員
interface Occupancy {
  count: number;
  densityPerM2?: number;         // 参考表示
  activityLevel: ActivityLevel;  // "office_work" | "cooking_service" | ...
  sensibleHeatPerPerson: number; // W/人(activityLevelから参照 or 上書き)
  latentHeatPerPerson: number;   // W/人
}

// 照明
interface Lighting {
  wattPerM2: number;
}

// 機器・厨房発熱
interface Equipment {
  items: EquipmentItem[];        // 積み上げ方式
  fallbackWattPerM2?: number;    // 積み上げがない場合の代替(要確認)
}
interface EquipmentItem {
  name: string;
  heatOutputW: number;
  quantity: number;
}

// 換気
interface Ventilation {
  outdoorAirVolume: number;      // m³/h
  ventilationType: "type1" | "type2" | "type3";
  heatExchanger: { enabled: boolean; efficiency?: number };
}

// すきま風条件
interface InfiltrationCondition {
  sashTightness: "good" | "medium" | "poor";
  hasExternalDoor: boolean;
  doorFacesWind: boolean;
}

// 気象データ
interface Weather {
  cityId: string;
  designDBtSummer: number;
  designWBtSummer: number;
  designDBtWinter: number;
  designRHWinter: number;
  solarByOrientation?: HourlySolarData[]; // EA気象データ由来(未取得時はnull)
  dataSource: "EA気象データ" | "本アプリ暫定値(要確認)";
}

// 負荷計算結果
interface LoadResult {
  roomId: string;
  cooling: {
    structural: number;
    glass: number;
    solar: number;
    lighting: number;
    occupantSensible: number;
    occupantLatent: number;
    other: number;
    infiltrationSensible: number;
    infiltrationLatent: number;
    outdoorAirSensible: number;
    outdoorAirLatent: number;
    total: number;              // 時刻別最大値
    peakHour?: number;
  };
  heating: {
    structural: number;
    glass: number;
    infiltrationSensible: number;
    infiltrationLatent: number;
    outdoorAirSensible: number;
    outdoorAirLatent: number;
    total: number;
    peakHour?: number;
  };
  calculationMode: "simple" | "detailed";
}

// 機器選定
interface EquipmentSelection {
  requiredCoolingKW: number;      // LoadResult.cooling.total由来
  requiredHeatingKW: number;
  ratedCapacityCorrection: {      // §8 ②
    method: "manufacturer_curve" | "none";
    correctedCoolingKW?: number;
    correctedHeatingKW?: number;
  };
  selectedUnitSizeKW: number;      // §8 ③
  selectedUnitCount: number;
  installedTotalKW: number;
  marginRatio: number;             // §8 ④(表示のみ)
}
```

---

## 11. 計算式ごとの出典対応表

| 計算式 | 出典 |
|---|---|
| 冷房負荷8項目・暖房負荷5項目の定義 | 建築設備設計基準(国土交通省、令和6年版)第4編第1章第2節(cite index="49-1">) |
| すきま風負荷の算定式(換気回数法・単位すき間風量法) | 建築設備設計基準(cite index="47-1">) |
| 熱源機器容量は建物時刻別負荷集計の最大値に基づき算定 | 建築設備設計基準(cite index="49-1">) |
| 機器定格能力の試験条件(冷房27℃DB室内/35℃室外、暖房20℃室内/7℃・2℃室外) | 日本冷凍空調工業会(JRAIA)公式サイト(cite index="62-1">) |
| 設計用外気条件・方位別日射量データの位置づけ | 拡張アメダス気象データ(EA気象データ)、MetDS社、SHASE HASPEEでの採用実績(cite index="68-1,72-1,74-1">) |
| 人体発熱・照明W/m²等の目安値 | 実務解説記事(株式会社パラダイム)(cite index="53-1">。**公的基準ではなく、参考値である旨を画面表示でも明示します**</cite> |
| 外気負荷の顕熱・潜熱計算式(湿り空気の基本式) | 空気調和工学の一般的な計算式(比熱・エンタルピーによる熱量計算)。個別の公的資料は今回未特定のため、実装時にSHASE便覧等で数値係数(空気の比熱・密度等の採用値)を確認する必要がある |
| 構造体貫流負荷の実効温度差(ETD)法 | SHASE関連資料(HASPEEの前身であるDLC解説等で言及)(cite index="46-1">。**本アプリでは原文未確認。実装前にSHASE便覧での確認が必要** |

---

## 12. 現在の28.1kWは詳細モードでどう変わるか(具体例)

現行値(飲食店・延床100m²・在室30人・関東・冷房26℃/暖房22℃設定)は、面積原単位法(244W/m²)による概算値です。詳細モードで同じ建物を計算する場合、次のような入力が必要になり、結果は入力次第で変わります。

### 想定例A:窓が少なく断熱性能が高いテナント(郊外の新築ビル1階、窓面積小)

- 窓面積:正面(南面)のみ15m²、Low-Eペアガラス(U値低め、SC値小さめ)
- 外壁:高断熱仕様(U値小さめ)
- 厨房機器発熱:換気フードで局所排気されており、室内負荷への影響を機器積み上げで小さめに算定
→ この場合、構造体・ガラス面・日射負荷が現行の原単位法より小さく算定され、**28.1kWより低い値になる可能性があります。**

### 想定例B:全面ガラス張りの路面店(西日を受ける、断熱仕様不明)

- 窓面積:西面・南面合わせて40m²、単板ガラスまたは仕様不明のため安全側にU値・SC値を大きく設定
- 厨房機器発熱:機器積み上げをせず、換気フード風量からの逆算で大きめに見積もる
- すきま風:出入口の扉が風上側にあり、換気回数を夏期2回で計算
→ この場合、日射負荷・ガラス面負荷・すきま風負荷が現行の原単位法より大きく算定され、**28.1kWより高い値になる可能性があります。**

いずれのケースも、**実際の数値は建物仕様(窓面積・断熱仕様・方位・厨房機器の実発熱)を入力しない限り確定しません。** 現行の28.1kWは「原単位法によるレンジの目安の1点」であり、詳細モードは「その1点が実際にはどちらの方向にどれだけずれうるか」を建物仕様に基づいて示すためのものです。

---

## まとめ・次のアクション(提案)

1. この設計書の方向性がご確認いただければ、次は「UIワイヤーフレーム(詳細モードの入力画面構成)」の設計に進めます。
2. 実装前に必須で解決すべき事項:
   - EA気象データ(拡張アメダス気象データ)をライセンス契約するか、暫定の簡易テーブルで代替するかの方針決定
   - SHASE「空気調和・衛生工学便覧」原文の入手(人体発熱表・ETD法の正式な数式・空気物性値の採用係数を確認するため)
   - メーカー別の機器能力補正曲線データの収集方法(§8②)

コードの変更は、この仕様書についてご確認いただいた後に着手します。
